# Claude Code Integrity And Observability Optimization Program

Date: 2026-07-16

Release disposition: the bounded Claude Code/Codex integrity and local webhook
observability slice is accepted for the `v0.1.6` source-preview release. The
next-release register below is intentionally outside this release boundary; it
must not be read as a blocker for the accepted slice or as completed support.

Historical program status and evidence chronology: Phase 0, the fixture-backed `CC-13`
terminal-failure slice, the deterministic configuration portion of Phase 2, and
the isolated Claude Code 2.1.207 native census for `CC-00`, `CC-01`, `CC-03`,
`CC-04`, `CC-05`, `CC-11`, `CC-12`, `CC-15`, and `CC-16` are complete. The
implemented root completion, request-purpose, customer-usage, live lifecycle,
causal-write, and commit-projection slice has also passed a real Claude Code
2.1.207 managed full-stack acceptance run. The broader program remains active
for the explicitly listed residual gaps. The `CC-06`, `CC-06-R`, and `CC-06-RR`
live runs all failed. The later `CC-06-RRR` run passed the repaired core for
human/task provenance, ghost-root exclusion, three exact parallel children,
request conservation, lifecycle ordering, repository binding, and privacy. Its
post-run audit still found provider-neutral update-order, cross-run delivery,
diagnostic-retention, and expanded-snapshot cost-coverage release barriers.
The rebuilt `CC06B-20260713T060835Z` run is live evidence for the strict public
update clock and inclusive diagnostic-window retrieval, while the current full
gate passes the JavaScript/TypeScript suite, metadata census, TypeScript and Go
build/test gates, and high-severity dependency audit.
Claude emitted three serial Agent-launch assistant messages rather than the
requested one-message tool batch, so that batch shape remains unproven. The
later controlled `CC15A`/`CC15B` egress probe held one root's `run.update` HTTP
response open while a distinct fresh root started and delivered its trusted
terminal in 3.139 seconds; the cross-run lifecycle-anchor bypass is now
live-accepted. This bounded dispatch result does not make a broader readiness
claim.
The deterministic connected late-decision egress gate now also passes: a
disposable watched repository, actual attribution/dispatch services, real Git
reconciliation, and a metadata-only loopback receiver prove both the eligible
late correction and the post-boundary rejection fence. It constructs safe
observations directly, so it does not replace live Claude emission or
`TelemetryIngress` acceptance.
`CC15C` is now prepared as the narrowly scoped live bridge between those two
evidence sets. Its first two separately authenticated Claude Code 2.1.210
attempts reached the relay-held gate but were inconclusive. A third reached the
expected hook-cardinality checks and an internally recognized native rejected
Write, but failed the strict relay binding gate. Static review of the pinned
2.1.210 exporter found the harness had explicitly set
`OTEL_METRICS_INCLUDE_SESSION_ID=false`; despite its name, that setting strips
the fresh session from native logs and traces as well as metrics, making the
required exact session join impossible. That is a harness defect, not
acceptance evidence. The harness now explicitly enables the fresh local session
identity for this loopback-only probe, keeps it transient and never exposes it
in status/output, treats cross-surface prompt-ID variance as diagnostic-only,
and can hold one early native candidate until the exact controlled hook denial
arrives. Exact session, opaque `tool_use_id`, cardinality, source time, and
closed-root boundary remain hard gates. It mirrors the proven no-matcher
contract for `UserPromptSubmit` and `Stop`, supplies OTLP only in the Claude
child environment, explicitly owns generic and signal-specific OTLP
header/compression settings, and reduces relay failures to fixed metadata-only
ingress, binding-subreason, target-status-class, and unsupported-metrics
categories before cleanup. The opt-in harness runs an isolated direct agent,
metadata-only receiver, and loopback relay under a user-provisioned, separately
authenticated disposable `CLAUDE_CONFIG_DIR`. It
holds exactly one real native rejected-Write decision whose opaque `tool_use_id`
matches the controlled PreToolUse denial, until a baseline root-span terminal is
delivered. It then releases that decision and requires a later versioned terminal
with the exact safe rejection and no file, commit, execution-grant, completed
Write-hook, or post-terminal-update claim. It also fixes the disposable Git
HEAD, requires an exact closed `claude_code.interaction` trace joined to the
submission session, and rejects normal-profile aliases, containment, and proxy
escape. A later repaired rerun cleared the relay-held gate but reported no
receiver baseline run and the fixed local-only `provenance_unavailable` result.
Its subsequent rerun narrowed that to `provenance_origin_not_human_typed`: the
reader and candidate match succeeded, but the final exact human/typed pair did
not. A further rerun split that result to
`provenance_transcript_origin_kind_missing`. The existing CC15C invocation
explicitly pipes its prompt to Claude `-p`; the local CLI contract defines `-p`
as non-interactive, so this headless input cannot be upgraded to customer
authority from transcript shape alone. It remains a fail-closed negative
control. The harness now also has a TTY-only interactive mode for the next
native-provenance acceptance attempt. A TTY does not prove human identity;
`TelemetryIngress` still requires Claude's exact native human/typed evidence.
The first TTY run reached the controlled denial but then returned only the
fixed `native_decision_not_held_closed_root_trace_count` result. That strict
relay counter means the observed session-matched closed-root count was not one;
it deliberately does not expose whether the cause was no match, a replay, or
multiple roots, and it is evaluated before the native-decision time-boundary
test. Do not repeat CC15C merely to refine that harness counter. Keep this
late-decision route pending until a separately designed, metadata-only
interactive trace-topology census can prove a unique decision-containing root
  or reject the source shape. This does not relax production authority. Advance
  the independent capability-gated local-MCP `CC-18` transport lane instead;
  third-party OAuth coverage remains a separate later claim.

**Current prioritization (2026-07-16):** at the operator's direction, all CC15
late-decision work is deferred. CC15D remains the prerequisite if that lane is
resumed; it is not the next scheduled live run. The consolidated deferred-use-
case register below is the source of truth for the remaining program scope, and
the installed-agent interactive normal-root baseline `CCLIVEA-20260716T073022Z`
has passed as the immediate non-CC15 live acceptance. The later concurrent
webhook gate `REL-E2E-20260716T082716Z` closes the bounded release requirement;
it does not reopen CC15 or promote the next-release register into release scope.
Its deterministic shutdown protocol now releases, seals relay ingress,
drains the direct agent to a bounded ingress/runtime/webhook fixed point (across
OTLP/provider hooks and the optional local span-DB poller), seals
the receiver, and verifies both sealed endpoints again after orderly direct-agent
exit; slow in-flight relay and receiver regressions prove those seals wait rather
than snapshotting a request in progress. It does not prove live file/commit retraction,
because the live Write is denied before any file or commit can exist; that
stronger route remains covered by the deterministic connected gate.
`CC07RR` retained the Claude integrity and privacy checks but still missed the
five-second first-terminal SLO: a queued same-run `settling` snapshot was
selected before an already-due terminal. The terminal-admission repair remains
valuable, and the scoped terminal-selection priority hardening is now implemented
and deterministically verified. `CC07U` subsequently passed the five-second
source-boundary target after the native cadence repair, but its no-tool shape
does not replace the outstanding child-batch, correction, or bypass probes; no
broader readiness claim follows. `CC07RRR-20260713T080618Z` live-proved that the scoped terminal
priority now wins over a competing same-run settling row, but it is not an
acceptance run: the receiver was offline, Claude emitted one unrequested
`ScheduleWakeup`, and the closed root reached Tirion about five seconds after
the provider boundary. `CC07S-20260713T083510Z` later met the first-terminal
target, but that isolated pass did not prove that the generic batch setting
controlled every native trace path. `CC07T-20260713T090706Z` confirmed the
gap: with the managed generic setting still at 250 ms, the authoritative
closed-root trace arrived about five seconds late and the first terminal missed
the target by about 0.57 seconds. Claude Code 2.1.207's native enhanced trace
path reads `OTEL_TRACES_EXPORT_INTERVAL` directly and defaults it to 5000 ms.
The v4 source configuration now manages, validates, drift-checks, and
reversibly restores that native interval at 250 ms, while retaining the
compatible generic `OTEL_BSP_SCHEDULE_DELAY=250`. `CC07U-20260713T105000Z`,
after the required Claude reopen, accepted that source-latency repair: its
closed-root trace reached Tirion in 275 ms and its first receiver terminal in
3.252 seconds. It also passed one-root, no-tool, privacy, ordered-delivery, and
drained-outbox checks. This bounded no-tool run does not prove the requested
single parallel tool-call batch or a genuine eligible pre-boundary late
correction. `CC15A`/`CC15B` separately accept the unrelated in-flight delivery
bypass. `CC07T` remains the four-child/four-Read accounting and
post-boundary-contamination evidence.
`CC07V-20260713T114904Z` then exercised the requested four-child batch. Safe
native topology proves four direct Explore children under one root assistant
response, and the deduplicated activity projection retained four Explore and
four Read facts with no forbidden activity or privacy leakage. The Claude UI
provided the separate one-message/batch and one-read-per-child evidence; those
message-level claims are deliberately not reconstructed from timestamp-only
safe telemetry. Although all children returned, neither a root `Stop` nor
`StopFailure` arrived before or after the clean exit, so Tirion correctly
emitted no terminal rather than inventing one. Source configuration v5 now
manages Claude `SessionEnd` solely as a bounded diagnostic hook: it records only
the event category and allowlisted normalized reason in safe diagnostics and
cannot create a durable run fact or `run.ended`. Reopen Claude after applying
v5, `CC07W` accepted that isolated exit boundary at
`2026-07-13T12:56:01.478Z`: a fresh bare `/exit` produced one ignored
provider-hook diagnostic (`session_end` / `prompt_input_exit`) and no
provider-hook safe observation, production run, queue/delivery, or run webhook.
Concurrent native OTLP logs/traces included one log query occurrence, but
missing or ambiguous repository binding failed closed, so no public lifecycle
was projected. `CC07X-20260713T130423Z` then accepted the normal minimal
repository-bound root/exit route: one no-tool root reached the receiver as
ordered `run.start`, `run.update`, `run.ended` v1 at 3.099 seconds, and a
versioned `run.ended` v2 correction at 6.610 seconds; the later clean exit
emitted only the ignored `SessionEnd` diagnostic and did not create a duplicate
root or terminal. Privacy scans of the agent, structured logs, and receiver
found no prompt, reply, path, or content leakage. `CC07Y-20260713T131748Z`
then isolated the same gap with one background child: the child activity and
the provenance-resolved task notification correlated to the existing root, but
no eligible post-continuation ordinary Stop/closed-root pair arrived. The root
correctly remained open with no terminal. This is missing completion capture,
not authority to infer completion. On `prompt_input_exit`, the narrowed
hardening records only a bounded in-memory count of unresolved background roots
in the private `claude_background_root_missing_terminal` diagnostic. It never
creates a durable observation, repository binding, terminal, delivery, or
webhook. `CC07Z-20260713T134947Z` accepted that diagnostic boundary: one
background-child root remained open, recorded exactly one count-one private
warning at `prompt_input_exit`, and produced no terminal. The receiver saw one
start and six updates, with no terminal or duplicate delivery; privacy scans
remained clean. `CC07AA-20260713T140240Z` then accepted the complementary
normal background route: its first Stop carried one background task, a later
ordinary Stop and exact closed root completed the same run, and the receiver
saw one start, five updates, and one terminal with no duplicate identity. The
later manual `/clear` was an ignored `SessionEnd` clear receipt with no
missing-terminal warning. It did not hold a background-paused root through
`/clear`, so the focused pending-clear control remains open: clear immediately
after the background-bearing Stop, before an eligible ordinary Stop/closed-root
pair can arrive. `SessionEnd` remains diagnostic-only.

`CC07AB-20260713T142132Z` accepted that unresolved-background `/clear` control.
One background-bearing Stop paused the repository-bound root; a later ordinary
Stop still lacked its exact closed-root half and remained nonterminal. The
manual clear yielded exactly one ignored `session_end` / `clear` receipt, zero
missing-terminal warnings, zero clear-derived provider-hook observations, and
no terminal. The root remained open after clear. The active receiver retained
one start and 47 updates, zero terminals, one run identity, and no duplicate
event identities; all deliveries preceded clear and the outbox drained. Current
agent, structured-log, and active-receiver marker scans were clean. The next
focused probe is sequential-resume isolation: a new human submission after
clear must start a distinct root, while the older unresolved root may neither
absorb it nor terminalize it without its own exact authority.

`CC07AC-20260713T162236Z` accepted that sequential-resume isolation. The fresh
no-tool human root began after the clear boundary and completed only from its
own exact Stop/closed-root evidence. Its receiver lifecycle was one start, one
update, and one terminal with unique identities and a drained outbox. The older
CC07AB root remained open, had no later activity, and did not update or
terminalize the new root. Agent durable storage, structured logs, and the active
receiver had zero marker matches. Any future recursive lineage claim is limited
to a separately supported agent-team or workflow surface with exact native
parentage; ordinary nested subagent delegation is unsupported.

`CC08A-20260713T163925Z` established that ordinary Claude Code subagents are
not that surface: the root emitted one exact direct Explore child, no grandchild
identity, no grandchild Read, and no terminal; Tirion produced no ghost root or
nested ownership claim. The current official Claude Code subagent contract says
subagents cannot spawn subagents, so this is expected source behavior rather
than a Tirion lineage defect. Standard nested delegation is therefore an
explicit negative control. Any future recursive lineage claim must come from a
separately supported agent-team or workflow surface with exact native parentage.

Proposed branch: `codex/claude-code-integrity-optimization`

Baseline: the reviewed revision that lands the Codex webhook lifecycle
optimization spike. Keep the exact revision in version-control/task metadata,
not in this evidence document.

## New Task Kickoff Prompt

Use this prompt to start the dedicated task in Codex:

```text
Your task is to execute the Claude Code Integrity and Observability Optimization
Program in docs/spikes/claude-code-integrity-observability-program.md.

First read AGENTS.md, constructs.md, the program document, the Codex optimization
report at docs/spikes/webhook-lifecycle-optimization.md, and the current Claude
monitoring documentation at docs/harness-run-monitoring.md. Apply construct-first
thinking before proposing or editing implementation.

Create and work on branch codex/claude-code-integrity-optimization from the
reviewed revision that contains the Codex optimization work. Do not discard or
overwrite unrelated local changes. Record the installed Claude Code version in
the program evidence log; keep exact commit revisions in version-control/task
metadata rather than this document.

Carry the work through native-source discovery, root-cause fixes at the owning
constructs, deterministic fixtures, live Claude Code acceptance, full
verification, and an explainable outcome report. Do not copy Codex-specific
heuristics into Claude. Derive authority from Claude's documented and observed
native hooks, OTLP logs, and OTLP traces. Treat unsupported or ambiguous evidence
as unknown and fail closed.

The customer promise is that Tirion is a near-real-time, privacy-safe source of
truth for the complete coding-agent run: root work, tools, skills, MCP calls,
subagents, nested agents, token usage, estimated cost, outcomes, causal files,
and lifecycle timing. Prompt text, response text, tool arguments/output, command
text, file content, diffs, transcript paths, and absolute paths must never persist
or leave the machine.

Begin by reviewing the accepted evidence and the current outcome report, then
resume at the highest-priority unaccepted slice. Do not repeat an accepted
native discovery as if it were new evidence. Verify every new assumption against
a disposable native Claude run before changing production classification or
projection. Keep the program document's evidence log and decision log current
as discoveries are made.
```

## Mission

Make Tirion's canonical `run.start`, `run.update`, and `run.ended` webhooks as
fast, reliable, complete, and truthful for Claude Code as they are for the
accepted complex Codex runs.

Customers should be able to treat Tirion as a near-real-time source of truth for:

- one complete user-initiated run,
- root model work,
- every tool call,
- skill activation and skill-attributed model work,
- MCP calls,
- direct, parallel, and nested subagents,
- agent teams and workflows when supported by the installed Claude version,
- exact provider-reported token dimensions,
- estimated cost with explicit billing context and coverage,
- native success, failure, rejection, or unknown outcomes,
- causally proven repository-relative file changes,
- lifecycle timing, delivery timing, and correction state.

The program is successful only when complex native Claude Code runs agree with
the provider evidence ledger and remain protected by deterministic regression
tests.

## Product Promise And Non-Negotiables

These rules outrank convenience, apparent completeness, and harness symmetry:

1. Keep one provider-neutral webhook contract across Claude Code, Codex, Cursor,
   and GitHub Copilot.
2. Put Claude-specific meaning at source configuration, ingress, classification,
   and authority selection boundaries. Do not add a Claude-only webhook format.
3. Never persist or dispatch prompt text, assistant responses, last assistant
   messages, tool input/output, command text, file contents, diffs, transcript
   paths, raw telemetry, or absolute paths.
4. A sensitive path may be inspected transiently only to resolve a watched
   repository or derive an opaque artifact key. Discard it immediately after.
5. Do not parse free-form model text, tool output, or shell output to infer an
   outcome, child relationship, token count, or changed file.
6. Do not infer parentage from timing or same-name proximity when Claude exposes
   `agent_id`, `parent_agent_id`, span parentage, or `tool_use_id`.
7. Do not convert missing outcome evidence into success. Preserve `unknown`,
   `unknownCount`, and exact rejection evidence/counts exactly; never convert
   generic failure into rejection.
8. Do not count one request, tool, skill, or child twice when hooks, logs, and
   traces corroborate the same native identity.
9. Do not expose internal Claude processes or auxiliary sessions as customer
   runs. Their usage may be retained as run overhead only when exact native
   lineage proves that it belongs to a customer run.
10. Repository binding and causal file attribution fail closed when evidence is
    absent or conflicting.
11. All cost fields remain estimates. Provider-reported `cost_usd` is evidence
    for an estimate, not proof of final billed cost.
12. Source readiness requires functioning surfaces, not configuration text that
    merely looks correct.

## Why This Is A Separate Program

The Codex program proved that webhook delay and incorrect payloads often begin
upstream of dispatch. Source configuration, identity, completion, usage
authority, child lineage, activity classification, repository binding, and
storage scheduling can each create an apparently similar receiver symptom.

Claude Code should therefore be investigated as its own provider contract. It
has stronger native request, tool, skill, and subagent evidence than current
Codex, but it also has provider-specific behaviors that make direct reuse of
Codex rules unsafe:

- `Stop` and `SubagentStop` are stop attempts that other hooks may block.
- `Stop` may report active background tasks and scheduled continuations.
- `StopFailure` fires instead of `Stop` for terminal API errors.
- one prompt has a native `claude_code.interaction` root span,
- subagent spans nest under the spawning Agent tool span,
- `agent_id` and `parent_agent_id` provide native hierarchical identity,
- `tool_use_id` joins hooks, logs, and traces,
- `claude_code.skill_activated` explicitly reports skill invocation,
- `workflow.run_id` can link workflow agents and nested skill work on supported
  versions.

The strategy is to preserve the shared Tirion constructs while deriving Claude
authority from Claude evidence.

## Codex Learnings To Carry Forward

### 1. Fix the owning construct

The Codex issue was not one dispatch defect. Real runs exposed defects in source
readiness, prompt correlation, internal-session filtering, completion authority,
child folding, token authority, outcome semantics, file evidence, queue priority,
and historical replay. Each fix belongs in the construct that owns the job.

### 2. Native evidence beats naming and timing

Exact native request IDs, call IDs, sessions, child IDs, and trace parentage are
authorities. Names and timestamps are corroboration only. Ambiguity must reduce
coverage rather than trigger a guess.

### 3. A model response is not a run boundary

A complex run contains multiple API responses, tools, and child turns. The run
ends only at a provider-authoritative root boundary. For Claude, the program
must determine the relationship among `Stop`, blocked Stop attempts, active
background tasks, `StopFailure`, and the closed `claude_code.interaction` span.

### 4. Child usage must be folded exactly once

Linked child work remains visible inside the root but must not also appear as a
standalone public run. Same-name child aggregation is complete only when every
represented child has exact lineage.

### 5. Usage authority is selected before aggregation

Choose the strongest usage surface independently for root and child work. Then
fold children into the root. Do not combine overlapping log and trace copies of
the same request.

### 6. Every token dimension must conserve

Input, output, cache read, cache creation, reasoning when present, and total
tokens must reconcile at the run and activity levels. Unallocated usage stays
explicit instead of being assigned to a convenient tool or child.

### 7. Unknown is an integrity result

Codex shell status demonstrated that protocol success is not process success.
Claude exports stronger tool outcomes, but any missing native field must still
remain unknown.

### 8. File attribution must be causal

Successful Edit or Write evidence can prove a changed artifact only when
workspace evidence retains the bounded opaque `{ artifactKey, executionNodeId }`
pair from its exact successful semantic-write node. Ordinary durable lifecycle
and commit projections revalidate that pair against the exact same-query/
repository durable execution-node record: it must be that node, contain the
allowed artifact evidence, and be a successful allowlisted semantic-write tool.
A missing, malformed, or unreadable exact proof fails closed. The sole live
provisional exception is the direct retained source node itself; a raw key,
generic causal-key set, or grouped activity outcome cannot substitute for it.
A dirty-worktree snapshot or nearby timestamp also cannot substitute for the
pair. Bash-created files remain unclaimed unless a native, structured surface
proves the exact path and call relationship.

### 9. Internal work needs durable exclusion

Dropping one internal observation is insufficient. A durable privacy-safe marker
must prevent later logs, traces, hooks, rebuilds, restarts, or attribution from
promoting internal work into a public run.

### 10. Real-time delivery needs independent lanes

Accepted lifecycle work must not wait behind usage rebuilds, workspace scans,
commit reconciliation, historical replay, or a stale outbox snapshot. Fresh
starts and terminals need bounded, priority-aware work paths.

### 11. First terminal and final truth are separate concerns

A fixed first-terminal deadline protects latency. A bounded, versioned
correction can improve usage, activity, models, or files without making the first
terminal wait indefinitely. `final` coverage identifies the authoritative
boundary represented by a terminal version; it does not prevent a higher
replacement when late-delivered evidence began on or before that boundary.

### 12. A successful live run is evidence, not regression protection

Every defect discovered by a native run should produce a versioned synthetic
fixture and a deterministic assertion at the lowest responsible boundary.

## Starting Baseline

### Current environment

- Claude Code installed when this document was written: `2.1.201`.
- Official documentation says `workflow.run_id` requires Claude Code `2.1.202`
  or later. Re-check both facts at program kickoff.
- The later native acceptance matrix ran on Claude Code `2.1.207`. That version
  is workflow-eligible by the documented minimum, but workflow/team behavior is
  still untested and unsupported by Tirion production classification.
- Current worktree branch when this document was written:
  `codex/webhook-lifecycle-optimization-spike`.
- The Codex spike and new complex-run fixtures were not yet committed when this
  document was written. Create the Claude branch only after establishing an
  intentional reviewed baseline.

### Existing Claude support

Tirion currently:

- manages Claude Code settings in `~/.claude/settings.json`,
- configures OTLP logs and traces to the local agent,
- configures `UserPromptSubmit`, `Stop`, `SubagentStart`, `SubagentStop`,
  `PreToolUse`, `PostToolUse`, and `PostToolUseFailure` hook relays,
- accepts `claude_code.user_prompt`, API request, tool result, and compatible
  trace shapes,
- keeps prompt capture, tool content, and raw API bodies disabled by default,
- treats Claude request evidence as higher usage authority than model/event
  copies,
- runs read-only, multi-file write, commit, privacy, and lifecycle scenarios in
  `local-harnesses/tirion_local_server_dual_provider_lifecycle_test.sh`,
- has provider-native activity and skill probes under `local-harnesses/`.

### Baseline gaps and current disposition

These were confirmed at kickoff. Resolved items remain recorded so the baseline
and the checkpoint do not contradict each other:

1. **Resolved in this checkpoint:** `TelemetrySourceConfiguration` set
   `OTEL_TRACES_EXPORTER` but did not manage
   `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1`, which the official monitoring
   contract requires for spans.
2. **Resolved end to end for the accepted root slice:** source status could
   claim traces were configured without proving the enhanced gate was enabled.
   Deterministic tests, isolated 2.1.207 native receipt, managed full-stack
   receipt, and the repaired persistent user source now verify the enhanced
   trace gate and redaction settings.
3. **Resolved for the accepted non-interactive root slice:** the reviewed
   baseline promoted every Claude `Stop` directly to completion. Native `CC-11`
   now proves a blocked Stop has
   `stop_hook_active=false` and a later final Stop has
   `stop_hook_active=true`; `CC-12` proves a Stop with one background task plus a
   closed interaction is still non-final. The bounded candidate/closed-root
   implementation is fixture-backed and passed the managed full-stack root
   lifecycle. A purpose or Stop join split by agent restart remains unsupported.
4. **Resolved for the fixture-backed `authentication_failed` slice:** Tirion did
   not configure or classify Claude `StopFailure`. Other native categories and
   restart between submission and failure remain open.
5. **Open:** production classification does not yet consume `parent_agent_id` or
   `workflow.run_id` as first-class lineage.
6. **Native presence/absence proven; production integration open:** `CC-15`
   emitted no `claude_code.skill_activated`, while `CC-16` emitted exactly one.
   Request-level skill ownership and token attribution are not yet accepted.
7. **Partially resolved:** the maintained Claude lifecycle system now accepts
   root read/write completion, exact session-title exclusion, and causal commit
   projection. Failed `CC-06`, `CC-06-R`, and `CC-06-RR` attempts produced
   deterministic repairs for transcript-authorized query lifecycle, direct
   Agent/child owner aliases, honest coverage, and concurrent webhook
   projection. `CC-06-RRR` then passed that bounded live core with one public
   root and three exact same-name children. The rebuilt `CC06B-20260713T060835Z`
   run live-verified strict update ordering and windowed diagnostics, but it
   emitted three serial Agent-launch messages rather than one batch and did not
   exercise an unrelated in-flight delivery. The requested batch shape remains
   open; `CC15A`/`CC15B` later accept the cross-run lifecycle-anchor bypass. The later
   `CC07R-20260713T062309Z` run retained one root, four exact direct children,
   exact accounting, and clean privacy boundaries, but its first terminal reached
   the receiver 9.142 seconds after the trusted source boundary. That is a
   failed terminal-latency result, not a release pass; deterministic
   terminal-admission hardening still needs a rebuilt live rerun. Nested agents,
   workflow/team work, exact skill-token attribution, interruptions, and retry
   exhaustion remain open.
8. The metadata-only census supplies safe evidence for one direct child, and
   deterministic production regressions now cover the exact direct Agent
   response/start bridge, parent-first tool ownership, all-member component
   aliases, and whole-component child deduplication. The bounded direct parallel
   case passed live in `CC-06-RRR`; child-first split ordering, recursive/nested
   ancestry, and restart/eviction behavior remain unaccepted.
9. Native model-driven shell scenarios remain opt-in. The final accepted
   full-stack run used the fixed built agent binary, while the metadata reducer's
   deterministic privacy/adversarial self-test and the discovered regression
   cases are part of the default `npm run check` gate.

### Execution checkpoint: 2026-07-12

The explainable checkpoint report is
`docs/spikes/claude-code-integrity-observability-outcome.md`.

- Working branch: `codex/claude-code-integrity-optimization`.
- The task began from the reviewed Codex optimization baseline; exact revisions
  remain in version-control/task metadata rather than this document.
- Installed Claude Code at kickoff: `2.1.201` (native arm64). The successful
  native matrix later ran on `2.1.207`.
- Node: `v24.16.0`; npm: `11.13.0`; Darwin: `25.4.0` on arm64.
- Auth initially reported first-party `claude.ai` Pro subscription, then changed
  to `loggedIn: false` / `authMethod: none` before the first successful run. The
  operator later re-authenticated explicitly; the 2.1.207 matrix ran as
  first-party `claude.ai` Pro with exact requested model `claude-sonnet-5`.
- `npm run check` passed from the clean baseline: 560 tests, all TypeScript
  builds, Go TUI build/tests, and the high-severity dependency audit with zero
  reported vulnerabilities.
- The maintained Claude lifecycle harness reached repository activation but the
  first read-only CLI scenario exited before prompt execution with
  `Not logged in`. The direct native activity probe failed at the same preflight.
  All private temporary capture artifacts were deleted.
- Safe inspection of the pre-existing Claude settings found logs and traces
  pointed at loopback Tirion endpoints, but the enhanced-trace gate and
  explicit assistant-response gate were absent. The managed hook set also lacked
  `StopFailure` and `SubagentStart`.
- Phase 2 source-readiness work now manages the enhanced-trace gate, explicitly
  privacy-closes assistant responses, installs `StopFailure`, checks the exact
  managed lifecycle hook shape and local hook policy, migrates legacy restore
  ledgers, fails closed on unrecoverable adoption, preserves mixed foreign hook
  handlers, and restores prior `0`/`1`/absent values. These changes are
  deterministic-test backed. The isolated 2.1.207 census first proved real
  loopback hook, log, and enhanced-trace receipts. The final fixed-binary
  acceptance then exercised Tirion-managed settings, durable projection, and
  webhook delivery end to end.
- An adopted header or preserved hook authorization is now immutable when its
  prior secret was deliberately not recorded. Agent-token rotation fails before
  mutation in that case; a fresh Tirion-owned configuration remains safely
  rotatable and reversible.
- The existing native probe scripts were not accepted as Phase 1 evidence
  infrastructure because they persist raw OTLP, headers, CLI output, and
  content-bearing summaries. A loopback metadata-only reducer now aliases
  identities and writes allowlisted safe records only; its deterministic
  self-test is part of `npm run check`.
- The opt-in `CC-13` run requested exact model `claude-sonnet-5` with an isolated
  synthetic invalid API key. Claude Code 2.1.201 emitted one matching
  `StopFailure(error=authentication_failed)`, no `Stop`, two failed `401` LLM
  requests (Sonnet plus auxiliary Haiku title generation), two `api_error` logs,
  and one closed interaction. No raw payload, header, CLI output, path, prompt,
  response, error detail, or raw ID was retained.
- That accepted snapshot included auxiliary Haiku title generation, but repeated
  pinned runs show that title request is optional. The stable CC-13 census
  invariant is exactly one failed requested-model span, with zero or one failed
  Haiku title trace child; any other model or request count fails closed.
- That fixture now drives one idempotent failed terminal occurrence and an
  optional provider-neutral `run.ended.outcome: "failure"`. Ordinary `Stop`
  remains outcome-unproven, and no other request/tool/skill/lineage hypothesis
  was promoted.
- The accepted zero-usage failure survives a later agent restart through the
  durable occurrence ledger without inventing a model, tokens, or cost. Recovery
  is bounded by the active production epoch and recent terminal window; retained
  provider terminal timing stays high-confidence while the retrospective event
  is explicitly marked delayed.
- Claude Code 2.1.207 then passed the metadata-only `CC-00`, `CC-01`, `CC-03`,
  `CC-04`, `CC-05`, `CC-15`, and `CC-16` scenarios with one prompt-hook/log/root
  bridge, the expected tool or skill outcome, one closed interaction, and the
  expected final Stop. `CC-11` observed one blocked Stop with
  `stop_hook_active=false`, followed by one allowed final Stop with
  `stop_hook_active=true`. `CC-12` observed one Stop with
  `stop_hook_active=false`, exactly one active background task, and one closed
  interaction, but no final completion.
- Every retained 2.1.207 run observed exactly one auxiliary
  `claude-haiku-4-5-20251001` request. Its `api_request` log carried
  `query_source=generate_session_title`, shared one request identity with its LLM
  span, and was a direct trace child of the root interaction. The cross-version
  census contract still permits zero or one such request.
- The production implementation introduces a provider-neutral usage purpose for
  exact session-title work, excludes only the proven request and its
  same-request signal copies from customer usage and live webhook projection,
  and keeps absent/unclassified purpose counted. Bounded
  prompt/session/trace/request correlation and Stop-candidate/closed-root
  handling are fixture-backed and accepted for the tested non-interactive root
  slice; the in-memory joins do not span an agent restart.
- The final fixed-binary managed acceptance ran real Claude Code 2.1.207 with
  exact requested model `claude-sonnet-5` for one read-only root and two writing
  roots. The harness passed its lifecycle, integrity, privacy, authorization,
  and metadata gates with 41 webhook events delivered and zero queued or
  blocked.
- Each of the three roots emitted `run.start`, `run.update`, and `run.ended`,
  including terminal versions 1 and 2. The two causal `commit.attributed`
  events contained only the writing run IDs and attribution-relevant trace
  subsets; the read-only run contributed neither commit identity nor cost.
- For the first and second writing roots respectively, each commit's
  `estimatedNanoUsd` and `usageValueNanoUsd` exactly equaled its writing run at
  85,806,600 and 108,632,400, with `costCoverage: "complete"`.
- Exactly three auxiliary request identities were retained in upstream safe
  evidence; none crossed the webhook boundary, and no Haiku model appeared in
  any delivered payload. An earlier live attempt exposed read-only cost
  inflation and reversed harness trace-subset assertions; both defects were
  fixed and now have deterministic regressions.
- Persistent user repair also succeeded: the agent is healthy on loopback port
  4318, Claude Code reports `managed_current` with all eight lifecycle/tool
  hooks plus enhanced-trace and redaction gates, a fresh exact-Sonnet receipt
  completed without a hook error, and doctor reports a healthy database with
  Claude `measurementState: "complete"`. The latest persisted Claude run has
  only `claude-sonnet-5`, 25 total tokens, complete cost coverage, and
  `auxiliary_session_title_excluded`; no Haiku model is present.
- `CC-06-RRR` (`CC06RRR-20260712T235657Z`) then exercised the repaired
  interactive three-child path. Post-response transcript provenance resolved
  the human/typed submission after 116 ms and a generated task/system
  notification after 108 ms. Only the human submission authorized a public
  root. The receiver recorded exactly one root and three exact Explore children;
  four real Bash calls remained honestly `unknown`, no file was claimed, the
  later away-summary request was excluded, and privacy scans remained clean.
- The final run total was 5,175 tokens (544 input plus 4,631 output), with
  586,625 cache-read and 79,344 cache-creation tokens. Root, grouped activity,
  and unallocated vectors conserved exactly; Explore coverage remained honestly
  partial rather than assigning the remainder. The first terminal arrived
  3.494 seconds after the provider boundary and the final correction arrived
  14.833 seconds after it; lifecycle ordering was monotonic and the durable
  queue drained.
- The same audit found three provider-neutral release barriers: 11 distinct
  update meanings shared one public `updatedAt`; the default 1,000-event
  diagnostic snapshot rolled the target window off within minutes; and the
  delivery design still allowed an unrelated in-flight update to hold a fresh
  lifecycle anchor. Strict logical update ordering, a bounded lifecycle-anchor
  bypass lane, and inclusive windowed reads over the bounded private structured
  logs were initially deterministic-gated.
- The rebuilt `CC06B-20260713T060835Z` live run subsequently verified a strict
  logical public update clock and retrieved 344 safe events through the
  inclusive diagnostic window. It had one human/submission root from
  06:10:09.692 through 06:10:28.870 UTC, with provenance resolving after 114 ms;
  its three real background Explore children were launched in three serial
  assistant messages rather than the requested single tool batch. The run did
  not create a second run with an unrelated response in flight, so it is not
  evidence for the lifecycle-anchor bypass.
- `CC07R-20260713T062309Z` subsequently retained one public root, four exact
  high-confidence Explore children, exact token/activity conservation, no causal
  files, clean privacy scans, and a drained outbox. Its first terminal reached
  the receiver 9.142 seconds after the trusted source terminal boundary, however,
  exceeding the five-second SLO. The closed-root authority was first observed
  4.140 seconds after the source boundary; normal live-update work and same-run
  delivery serialization added further runtime delay. Dedicated terminal
  admission, immediate Claude closed-root query projection, and durable queueing
  while an update is in flight are implemented deterministic hardening only; a
  rebuilt live rerun remains pending.
- `CC07RR` retained the integrity and privacy checks in a fresh Claude 2.1.207
  live rerun, but its trusted source boundary to first receiver terminal was
  13.622 seconds. The closed-root authority arrived 1.601 seconds after the
  source boundary, terminal meaning entered the durable outbox at 4.423
  seconds, and the terminal HTTP attempt did not begin until 9.733 seconds. A
  queued same-run `settling` update was selected before that due terminal. This
  is a dispatch-selection defect, not evidence to weaken the exact Claude
  Stop/closed-root authority. The deterministic rule applies only to this
  trusted Claude closed-root terminal (`codingHarness`/runtime `claude-code`,
  `evidence.basis: "root_span"`, `evidence.delayed: false`): it has selection
  precedence after fresh starts over fresh settling rows from the same or another
  run in ordinary and bounded-bypass lifecycle selection. An update already in
  flight remains serialized first, a failed/retrying terminal retains the
  same-run settling row as a fallback, and successful terminal delivery
  suppresses any remaining stale update. Focused deterministic regressions now
  verify this scope; normal completed delivery order is unchanged. A fresh live
  rerun remains pending to demonstrate the five-second target.
- Independent post-run review also found that merging a later expanded token
  snapshot could retain `complete` cost coverage from an older, smaller
  snapshot. Update and terminal merging now select cost evidence for the
  expanded totals and preserve `partial` coverage. This is deterministic
  hardening, not evidence produced by the `CC-06-RRR` binary.
- After the post-`CC-06-RRR` provider-neutral hardening, `npm run check` passes
  696 JavaScript/TypeScript tests across the root and workspaces, the metadata
  census gate, all TypeScript builds, Go TUI build/tests, and the high-severity
  dependency audit with zero reported vulnerabilities. The earlier 669-, 560-,
  588-, and 602-test checkpoints remain historical evidence below.
- Remaining unsupported or unaccepted scope is explicit: the available cloud MCP
  servers remain unauthenticated and no live `CC-18` result exists yet. A
  deterministic local capability-gated fixture now prepares that transport-only
  probe; it does not establish third-party OAuth semantics. Recursive and nested
  subagents, the distinct `CC-10` blocked/allowed `SubagentStop` scenario,
  workflow/team grouping, request-level skill-token ownership,
  compaction and auxiliary categories other than exact session-title generation,
  native purpose/ordinary-Stop joins spanning an agent restart, interruptions,
  retry exhaustion, interactive-mode native behavior beyond the accepted
  `CC-04I-R` and `CC-06-RRR` slices, and broader cross-version behavior still
  require their own evidence.
- After failed `CC-06` and `CC-06-R`, production Claude start authority now
  comes from a bounded trusted transcript tail: only human/typed creates a
  customer root, task-notification/system is correlation-only, and unavailable
  or ambiguous evidence fails closed. The root follows the resolved Claude
  settings path, including `CLAUDE_CONFIG_DIR`; path/symlink/race failures retain
  no transcript content. Raw and transcript aliases are preflighted atomically.
- After failed `CC-06-RR`, an unresolved synchronous submission returns 200 and
  retries only a bounded non-content locator/identity snapshot after the
  transcript row can become visible. Claude OTLP classification waits behind
  the same bounded pending-provenance barrier. ID-less matching requires a
  fresh transcript row even when an earlier prompt has identical content, and
  a bounded reservation/consumed-row ledger prevents overlapping or rapid reuse
  of the same transcript record. Overlapping unresolved submissions drop
  ambiguous buffered hooks. A single
  pending submission may replay only minimal Agent/subagent/Stop/StopFailure
  metadata after exact provenance resolution; replay failures are isolated,
  get one bounded durable retry, and cannot strand the provenance barrier. A
  closed interaction also waits through a 25 ms continuation-settle interval
  before joining the pending barrier.
- The query-scoped lifecycle repair keeps accepted, pending-terminal, and
  completed state on the opaque logical query. Completed aliases cannot reopen,
  and a later manual prompt releases only its own terminal halves. A
  background/cron Stop establishes the generated-task target across completion
  and the manual prompt; only that newer query's own background Stop replaces
  it. Exact trace-to-native-prompt identity alone suppresses the paused physical
  close.
- The deterministic lineage repair transiently consumes only exact Agent
  `tool_response.agentId` plus the allowlisted subtype, hashes the child
  identity, and bridges it to exact `SubagentStart.agent_id`; all other tool
  response/input content is discarded. Exact `tool_use_id`, not a wrapper span
  ID, owns the Agent activity and descendant usage. A bounded parent-first trace
  ledger and bounded same-request owner map fail closed on conflicts. Connected
  child copies are deduplicated as one whole component, every non-conflicting
  member ID remains a usage-owner alias, and incomplete exact-child ownership
  keeps coverage partial or unavailable with an explicit unallocated remainder.
  Ownership-conflict witnesses are monotonic across storage upserts and
  restarts; every signal copy of the conflicted request remains unallocated,
  and any affected tool or subagent group with separate valid usage is partial.
  Distinct atom revisions with the same stable Claude request identity reconcile
  once, select a deterministic revision, and union owner conflicts rather than
  summing the API request twice. Completion uses that selected reconciled
  surface, so a superseded open revision cannot mask the newer closed revision.
  A launch without exact child closure remains open with unknown outcome.
- Concurrent webhook repair coalesces pending completed meaning by run, gives
  fresh terminal work priority, and reconciles updates against the durable
  delivered high-water. Equivalent running meaning reuses one content-derived
  ID; changed meaning gets a new ID and stale unpublished rows are superseded.
  When distinct meanings share a provider source time, `evidence.observedAt`
  preserves that time while public `updatedAt` advances by a strict logical
  millisecond beyond the durable high-water. A bounded lifecycle-anchor bypass
  permits a fresh start or terminal to proceed while an unrelated ordinary
  update response is in flight; ordinary updates remain serialized.
  Update and terminal cost selection follows the chosen token totals, so an
  expanded partial-cost snapshot cannot inherit an earlier smaller snapshot's
  `complete` coverage.
- Those repairs are deliberately in-memory, with each correlation map capped at
  1,024 entries and oldest-entry eviction. Child-first split envelopes cannot
  revise an already emitted atom; eviction/restart loses the transient joins;
  recursive `parent_agent_id` ancestry and nested descendants remain
  unsupported. `CC-06-RRR` is live evidence for only the bounded direct-parallel
  core. `CC06B-20260713T060835Z` adds rebuilt live evidence for strict logical
  update ordering and inclusive diagnostic windows, but its serial launch shape
  and single-run delivery leave the requested one-message batch unproven.
  `CC15A`/`CC15B` separately accept the cross-run lifecycle-anchor bypass.
  Neither result establishes broader readiness.

### Hypotheses that require native evidence

The non-interactive 2.1.207 matrix proves one exact prompt-hook/log/root bridge
for each retained run and the `CC-11`/`CC-12` Stop states described above. It
does not generalize those observations to other modes, versions, or billing
contexts. The following remain unresolved:

- whether `UserPromptSubmit` arrives before the OTLP prompt event in every CLI
  and interactive mode,
- whether the tested blocked/final Stop ordering and interaction ordering remain
  stable in interactive mode and future Claude versions,
- whether child `agent_id` appears consistently in non-interactive and
  interactive traces for the tested auth context,
- whether child API request logs carry enough identity without enhanced traces,
- whether the deterministic exact Agent `tool_response.agentId` to
  `SubagentStart.agent_id` bridge and parent-first `tool_use_id` ownership hold
  for every child in the live `CC-06` rerun; trace `agent_id` and recursive
  ancestry remain unresolved,
- standard Claude Code subagents cannot spawn subagents, as confirmed by
  `CC08A` and the current official contract; whether a separately supported
  agent-team or workflow surface exposes recursive `parent_agent_id` remains
  unresolved,
- whether usage on compaction or other auxiliary requests belongs inside the
  customer run as overhead or must be excluded,
- whether native request-purpose or ordinary-Stop/closed-root correlation can be
  made safely durable across an agent restart,
- whether local capability-gated MCP success/failure exposes the exact `CC-18`
  identity and usage contract, followed separately by an approved third-party
  authenticated-MCP source,
- whether FileChanged can ever provide causal evidence rather than temporal
  observation,
- ordering and export delay outside the tested non-interactive CLI mode,
- whether subscription, Anthropic API, Bedrock, and Vertex modes expose the
  same cost and billing-context evidence.

## Official Claude Evidence Map

The official contracts were reviewed on 2026-07-12:

- [Claude Code hooks reference](https://code.claude.com/docs/en/hooks)
- [Claude Code monitoring and OpenTelemetry](https://code.claude.com/docs/en/monitoring-usage)
- [Claude Code subagents](https://code.claude.com/docs/en/sub-agents)
- [Claude Code tools](https://code.claude.com/docs/en/tools-reference)

Re-verify them when the tested Claude version changes.

| Native surface | Strong identity | Candidate Tirion role | Important caveat |
| --- | --- | --- | --- |
| `UserPromptSubmit` hook | `session_id`, hook receipt time | earliest live start anchor and repository context | prompt content and transcript path are sensitive; native prompt ID may arrive later through OTLP |
| `claude_code.user_prompt` event | `prompt.id`, session attributes, event sequence | durable query identity and hook reconciliation | event export can lag the hook |
| `claude_code.interaction` span | trace/span identity, interaction sequence | authoritative root topology and closed turn boundary | ordinary Stop still requires the exact same-root join; the current join is in-memory and restart-limited |
| `claude_code.api_request` event | `prompt.id`, `request_id`, model | exact request usage and provider-reported estimated cost | retries and trace copies must not duplicate one request |
| `claude_code.llm_request` span | `request_id`, `agent_id`, `parent_agent_id`, span parent | request usage, root/child ownership, outcome | enhanced traces must actually be enabled and emitted |
| `PostToolUse` hook | `tool_use_id`, tool name | successful tool activity and transient write-path evidence; for Agent only, exact response child ID may bridge to `SubagentStart` | only exact Agent child ID and allowlisted subtype may cross transient classification; discard all other sensitive input/output before append |
| `PostToolUseFailure` hook | `tool_use_id`, tool name | failure activity | a structured interruption may be rejected; generic failure remains failure |
| `claude_code.tool_decision` event | `tool_use_id`, exact decision | separate opaque permission-decision evidence | an exact `reject` is permission authority, not execution evidence. Matching uses opaque `invocationId` whenever either surface has one, with request identity only as a legacy fallback when both lack it. It owns no descendant usage, accounting, context, execution, or lifecycle authority; within retained correction time it may source-prune only its exact provisional activity/file proof when its source `startedAt` is at or before the retained explicit completion boundary. An otherwise exact post-boundary or malformed source is ineligible. |
| `claude_code.tool_result` event | `tool_use_id`, structured `success` | authoritative outcome and hook/log deduplication | rejected calls use a decision event rather than tool result |
| `claude_code.tool` and execution spans | `tool_use_id`, `agent_id`, span parent | duration, child ownership, structured tool topology | detailed attributes may be gated |
| `SubagentStart` hook | `agent_id`, `agent_type`, parent session | early child activity and name | not proof that child usage was exported |
| `SubagentStop` hook | `agent_id`, `agent_type` | child completion candidate and outcome attempt | can be blocked; response and transcript fields are sensitive |
| `agent_id` / `parent_agent_id` trace attributes | opaque agent IDs | exact direct and supported team/workflow recursive lineage | standard subagents cannot spawn subagents; any recursive surface must be version/account fixture-backed |
| `claude_code.skill_activated` | `skill.name`, trigger, source | exact skill invocation | custom names may be redacted unless tool details are enabled |
| Skill tool result/span | `tool_use_id`, `skill_name` | skill duration/outcome and request parent | merge with skill event, do not count twice |
| request `skill.name` | request identity plus skill name | exact skill-attributed model usage | one skill may span several requests |
| MCP tool names/attributes | `tool_use_id`, server/tool metadata | MCP activity and exact request lineage | custom names may be normalized or redacted |
| `workflow.run_id` | stable workflow run ID | workflow, teammate, nested agent, and skill grouping | official contract requires Claude Code 2.1.202 or later |
| `Stop` hook | prompt/session plus stop state | root completion candidate | may be blocked; active background tasks or crons mean the session is paused, not done |
| `StopFailure` hook | session plus structured error type | failed terminal candidate | fires instead of Stop for terminal API errors |
| Task and teammate hooks | task/team IDs | background and team lifecycle evidence | descriptions and messages are sensitive and must be discarded |
| Edit/Write path metadata | `tool_use_id` plus structured path | causal artifact key after success | raw/absolute path is transient only |
| `FileChanged` hook | watched path and session context | corroborating workspace evidence | file observation alone does not prove agent authorship |

## Proposed Authority Rules

These are the initial rules to validate, not permission to skip native discovery.

### Root identity and start

1. A repository-bound `UserPromptSubmit` hook creates the provisional live root
   only when bounded transcript provenance resolves human/typed. A generated
   task-notification/system hook is correlation-only.
2. The first trusted prompt anchor fixes `startedAt`; delayed OTLP evidence may
   strengthen identity but must not rewind an already-published run.
3. Reconcile the hook query with `prompt.id` and the interaction trace through
   exact session/trace facts. Missing or mismatched raw prompt ID may use one
   unique nearest same-session transcript record in the bounded window; a tie
   fails closed, and all prompt aliases are atomic.
4. Never require prompt text for identity.
5. Session processes such as `agents_view`, setup, compaction-only work, or
   other documented auxiliary activity must not create public roots.
6. Scope accepted and completed state to the logical query. The session's active
   entry is only a routing pointer: a generated notification uses the exact
   background-authorized target, completed aliases cannot reopen, and the
   remaining terminal half of an older query cannot complete a newer sequential
   human prompt.
7. An active submission hook outranks a delayed provider pointer for the same
   session. Delayed evidence may enrich only the exact identity it matches.

### Completion

1. Treat a Claude `Stop` as a high-confidence completion candidate, not
   unconditionally as final authority.
2. A Stop with active `background_tasks` or actionable `session_crons` keeps the
   run open or settling.
   Its physical closed interaction is nonterminal only when exact
   trace-to-native-prompt correlation identifies that paused prompt; do not use
   timing or session proximity.
3. A Stop or SubagentStop that another hook blocks must not close the root or
   child. Determine this from the subsequent native lifecycle and closed trace,
   not from response text.
4. A closed root `claude_code.interaction` span is authoritative when its native
   ordering proves the turn truly ended.
5. `StopFailure` should end the run as a failed provider termination when exact
   identity is available. Preserve structured error category only if allowed by
   the provider-neutral contract.
6. A user interrupt or missing completion surface must use an explicitly
   documented fallback and non-final coverage. Do not silently label it success.

### Usage and estimated cost

1. Sum each unique Claude API request once by stable request identity.
2. Reconcile log and trace copies of the same request. Prefer the surface with
   the complete token vector and strongest root/child lineage.
3. Track input, output, cache read, cache creation, reasoning if Claude begins
   exporting it, and total tokens independently.
4. Select authority separately for root and each child before folding.
5. Keep retries and terminal API errors from becoming successful duplicate
   usage rows.
6. Treat `cost_usd` as a provider-reported estimate. Keep expected billed cost
   unavailable when auth/billing context is unproven.
7. Auxiliary requests with exact run lineage may become explicit overhead or
   unallocated run usage. They must not become separate customer runs.
8. The 2.1.207 census proves `query_source=generate_session_title` identifies the
   optional Haiku session-title request and that its span is rooted directly
   under the interaction. The current patch excludes only that exact purpose
   from customer usage and live webhook facts; missing or unclassified purpose
   remains counted. Broader compaction and auxiliary-overhead policy remains
   pending.

### Subagents, teams, and workflows

1. Normalize hook/trace `agent_id` into an opaque child-session identity. For
   Agent `PostToolUse`, read exact `tool_response.agentId` and the allowlisted
   subtype only transiently, hash the identity, bridge it to exact
   `SubagentStart.agent_id`, and discard the rest of the response/input.
2. Use exact Agent `tool_use_id` as activity and descendant-usage ownership;
   never substitute the wrapper span ID. Parent-first span ancestry and a
   same-request owner bridge may establish exact direct lineage. Timing and
   same-name matching are not authority.
3. Merge every connected activity/request/child-session copy as one component,
   preserve every non-conflicting member activity ID as a usage-owner alias,
   then recursively fold only exact supported child links into the public root
   once. Exact child lifecycle owns semantic timing/outcome; a launch without it
   stays open/unknown even if a wrapper closes successfully.
4. Preserve child tools, failures, unknown outcomes, token vectors, and parent
   activity IDs.
5. A same-name group reports complete trace-descendant usage only when all
   represented children are exact and each has owned usage. Missing ownership
   is partial with an unallocated remainder; a conflict is unavailable.
6. Keep unmatched children separate or unavailable. Never force them onto the
   nearest root.
7. Add workflow/team grouping only after a supported native version and fixture
   prove `workflow.run_id` semantics.
8. Fail closed for child-first split envelopes, conflicting owners, eviction or
   restart of the bounded join state, and unaccepted recursive
   `parent_agent_id` ancestry. Standard Claude subagents cannot spawn subagents;
   retain that as a negative control. Do not claim recursive support unless a
   separately supported team/workflow surface passes native and live acceptance.

### Tools, skills, and MCP

1. Deduplicate ordinary hook, result-event, and span evidence by `tool_use_id`.
   A native permission decision keeps separately addressable opaque evidence.
   An exact cross-surface collision prefers `invocationId` whenever either
   surface supplies one; only when both invocation IDs are absent may the
   nonempty request identity be the legacy fallback.
2. `PostToolUse` plus native successful tool result is success.
3. `PostToolUseFailure`, failed tool result, or error span is failure unless a
   structured interrupt/rejection field proves rejection.
4. A validated native rejection may contradict a generic result only for the
   same provider/query/tool kind/name and the exact safe invocation identity:
   `invocationId` wins whenever either surface provides it, while request ID is
   a fallback only when both lack an invocation ID. It is not execution, cannot
   own descendant usage, accounting, context, or lifecycle authority, and
   retains no execution/result/audit/causal-write evidence. During the retained
   correction window, a late exact decision source-prunes only its matching
   provisional activity/file evidence only when its source `startedAt` is at or
   before the retained explicit completion boundary. An otherwise exact
   post-boundary or malformed source is ineligible. A durable grouped activity
   changes only through the authoritative completed-run rebuild, never through
   a broad direct prune.
5. Merge `skill_activated`, Skill tool, and skill-attributed requests into one
   semantic skill activity without double counting.
6. Attribute request tokens to a skill, MCP call, or subagent only with exact
   native ownership. Keep the remainder explicit.

### Files and commits

1. Successful structured Edit, Write, or NotebookEdit path evidence may create
   an opaque artifact key, but a key alone never authorizes `filesChanged`.
2. Ordinary durable file or commit projection requires the bounded
   `{ artifactKey, executionNodeId }` pair. Revalidate it against the exact
   same-query/repository durable execution node; only a successful allowlisted
   semantic-write tool node containing that artifact is authority. Missing,
   malformed, or unreadable proof fails external file and commit projection
   closed.
3. Rejected or failed writes create no causal artifact claim. A native rejection
   can contradict a generic success only under the exact invocation rule above.
   It source-prunes only its matching live provisional row/file during the
   15-second correction retention only when its source `startedAt` is at or
   before the retained explicit completion boundary; it cannot erase
   independently proven sibling writes or a durable grouped activity directly.
4. The sole live provisional exception is direct retained source-node proof:
   that same successful allowlisted semantic-write node must supply the
   artifact. It never accepts an unpaired raw key, generic causal-key set, or
   grouped activity result. A source-order race may therefore send an active
   version first and a later correction after the exact native decision arrives.
   A post-boundary or malformed source cannot revise the queued/delivered file
   or commit truth.
5. Historical internal allocation revokes only from a complete census carrying
   the explicit `nativeRejectedCausalWriteArtifacts` marker. Missing, empty, or
   unreadable marker evidence does not revoke allocation or a valid sibling
   writer.
6. If an existing commit event depended on an exactly pruned proof, its later
   version becomes `superseded`; no new unrelated event is invented, and a valid
   sibling writer remains active.
7. A Bash command, shell output, or nearby workspace delta is not exact path
   evidence. Do not parse command text to invent it.
8. FileChanged and snapshots may corroborate state and commit continuity but do
   not independently prove run authorship.
9. Resolve repo-relative paths only at outbound projection after `PrivacyGuard`.
10. Handle worktrees and multiple watched repositories record by record.
    Unknown or conflicting routes remain local.

## Construct Ownership

Keep the program inside existing constructs unless the JTBD genuinely changes.

| Construct | Claude optimization responsibility | Primary code |
| --- | --- | --- |
| `TelemetrySourceConfiguration` | enhanced-trace gate, explicit content gates, required hooks, version/source readiness, exact restore | `packages/agent/src/sourceConfiguration.ts`, `packages/agent/src/index.ts` |
| `TelemetryIngress` | bounded loopback receipt, per-record repository routing, transient workspace/path resolution | `packages/agent/src/otlpIngress.ts`, `packages/agent/src/index.ts` |
| `RunCorrelationLedger` | query-scoped accepted/completed aliases, active-session routing, exact terminal halves, trace-to-native-prompt pause identity, and bounded request/child correlation | `packages/engine/src/telemetryClassification.ts`, storage package |
| `TelemetryClassification` | Claude native schema to metadata-only lifecycle, usage, activity, and execution atoms; transient exact Agent response/start bridge and parent-first `tool_use_id` ownership | `packages/engine/src/telemetryClassification.ts` |
| `PrivacyGuard` | discard hook/OTLP content before durable append; validate outbound arithmetic and allowlist | `src/privacy/privacyGuard.ts` |
| `UsageProjection` | whole-component child deduplication, exact lifecycle/open-launch authority, supported-link folding, request deduplication, skill/MCP usage, and token conservation | `packages/engine/src/shadowUsage.ts`, `packages/agent/src/productionUsageService.ts` |
| `RepositoryObservation` and attribution | watched repo identity, opaque artifact keys, causal files, worktrees, commits | `packages/agent/src/repositoryObservationService.ts`, `src/attribution/` |
| `ExternalWebhookDispatch` | immediate lifecycle, fixed terminal deadline, per-run ordering, corrections, durable retry | `packages/agent/src/externalWebhookDispatch.ts` |
| `Diagnostics` | source, observation, projection, queue, delivery, and coverage latency without raw content | `packages/agent/src/index.ts`, contract diagnostics |

If construct ownership or a cross-construct contract changes, update
`constructs.md` in the same change.

## Investigation Method: Four Ledgers

Every native acceptance run should be reconciled across four independent
ledgers:

1. **Provider hook ledger**
   - event name,
   - receipt timestamp,
   - opaque session/request/agent IDs,
   - tool/subagent names,
   - structured outcome state,
   - background task counts,
   - no content fields in retained evidence.
2. **Provider OTLP ledger**
   - interaction trace topology,
   - prompt ID,
   - request IDs and token vectors,
   - tool-use IDs and outcomes,
   - agent/parent-agent/workflow IDs,
   - skill and MCP identities,
   - source export timestamps.
3. **Tirion durable safe ledger**
   - query occurrences,
   - usage atoms,
   - activity atoms,
   - execution nodes,
   - repository key and opaque artifacts,
   - production run projection,
   - outbox rows and diagnostics.
4. **Receiver ledger**
   - ordered canonical lifecycle events,
   - versions and event IDs,
   - root and child activity,
   - exact token dimensions,
   - models and estimated cost coverage,
   - files,
   - delivery timestamps.

The live-run report must show where every receiver value came from and where any
provider evidence was intentionally excluded.

### Privacy handling for native capture

- Capture raw provider payloads only in a disposable private temporary directory.
- Never add raw payloads, transcripts, prompts, responses, commands, tool bodies,
  or file contents to git.
- Derive a metadata-only fixture with synthetic IDs and canary values.
- Scan the Tirion database, logs, diagnostics, receiver, and fixture output for
  every canary.
- Delete the raw capture after deriving and reviewing the safe fixture unless an
  explicit local retention policy says otherwise.

## Program Phases

### Phase 0: Establish a reproducible baseline

Tasks:

- land or intentionally baseline the Codex optimization work,
- create the Claude branch from that reviewed revision,
- record Claude version, Node version, OS, auth mode, and model; keep the exact
  git revision in version-control/task metadata rather than this document,
- run `npm run check`,
- run current Claude read-only and write lifecycle scenarios,
- snapshot Tirion-managed Claude source status and existing user settings
  ownership without storing secrets,
- confirm the webhook receiver and agent begin with empty queues.

Exit criteria:

- the reviewed baseline and environment are recorded without embedding an exact
  commit SHA in this document,
- current tests pass,
- current behavior and known failures are reproducible,
- no unrelated local changes are discarded.

### Phase 1: Native Claude source census

Start with native capture before production edits.

Scenarios:

- one read-only prompt,
- one successful Read tool,
- one successful Edit or Write,
- one failed Bash or tool operation,
- one rejected/denied tool operation,
- one direct subagent,
- one skill invocation and one control run where the skill exists but is unused.

Use and extend:

- `local-harnesses/provider_native_activity_surface_test.sh`,
- `local-harnesses/provider_native_skill_usage_test.sh`,
- `local-harnesses/provider_skill_activity_stress_test.sh`,
- `local-harnesses/otlp_capture_receiver.mjs`.

Deliverables:

- a versioned Claude native surface inventory,
- ordering and latency table for hooks/logs/traces,
- exact field availability by scenario and CLI mode,
- safe synthetic classification fixtures,
- a list of facts, unsupported fields, and unresolved hypotheses.

Exit criteria:

- every proposed accepted field is fixture-backed,
- no production authority is based only on documentation,
- content-bearing fields and privacy handling are enumerated.

### Phase 2: Source readiness and privacy gates

Tasks:

- manage and restore `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA`,
- status-check the gate independently from exporter text,
- explicitly keep prompts, assistant responses, raw API bodies, and tool content
  disabled,
- decide whether `OTEL_LOG_TOOL_DETAILS=1` is required for exact skill, MCP,
  subagent type, and write path metadata,
- add only lifecycle hooks proven necessary, beginning with `StopFailure`,
- preserve foreign exporters and existing hook ordering,
- report partial or unavailable readiness when a required surface is absent,
- verify config drift and restore behavior against pinned versions.

Exit criteria:

- activation never reports trace readiness without a real trace-capable setup,
- configure/status/restore tests cover every owned field,
- a real prompt produces expected loopback receipts,
- no content gate is enabled accidentally.

### Phase 3: Root identity and lifecycle authority

Tasks:

- reconcile UserPromptSubmit, `prompt.id`, and interaction trace identity,
- fix `startedAt` to the first trusted prompt anchor,
- classify blocked Stop and active-background Stop as non-terminal,
- classify closed interaction and final allowed Stop ordering,
- add `StopFailure` and interruption coverage,
- persist an internal marker for non-customer Claude processes,
- keep restart identity stable,
- prove one public root under delayed/out-of-order evidence.

Exit criteria:

- exactly one public run per accepted human/typed submission and zero public
  runs for generated task notifications,
- no response/API call ends the run,
- blocked or paused Stop does not emit a false terminal,
- a final terminal arrives after real completion,
- internal processes never become public later.

### Phase 4: Child, team, and workflow lineage

Tasks:

- map hook and trace `agent_id` to one opaque child identity,
- retain exact `parent_agent_id` recursively only for a native surface that
  supports nesting; standard subagents are a negative control because they
  cannot spawn subagents,
- join Agent tool `tool_use_id` with child spans and hooks,
- reconcile start/stop revisions without double counting,
- fold direct children and only source-supported team/workflow nested children
  exactly once,
- preserve unknown outcomes when stop completion is not proven,
- test same-name groups, standard nested-delegation rejection, and
  ambiguous/missing links,
- upgrade Claude deliberately before testing `workflow.run_id` features,
- add team/workflow support only after native fixtures pass.

Exit criteria:

- one, three, and four direct-child scenarios conserve exactly; standard nested
  delegation produces no invented grandchild, and any supported team/workflow
  recursive scenario must conserve exactly,
- child public runs are suppressed only with exact lineage,
- partial linkage remains unavailable,
- team/workflow identities do not leak into unrelated roots.

### Phase 5: Usage, models, context, and estimated cost

Tasks:

- deduplicate API request log and LLM trace copies by request identity,
- verify retry behavior and failed requests,
- select root and child authorities independently,
- conserve all available token dimensions,
- retain exact model sets and provider identity,
- classify subscription/direct/Bedrock/Vertex billing context only from evidence,
- preserve provider `cost_usd` as an estimate,
- define explicit handling for auxiliary and compaction usage,
- ensure activity-level usage sums to the top-level run.

Exit criteria:

- Tirion totals equal the native Claude request ledger,
- no retries or corroborating surfaces double count,
- root plus children equals the public run in every dimension,
- incomplete billing evidence never produces confident billed cost.

### Phase 6: Tools, skills, MCP, outcomes, and files

Tasks:

- merge tool hook/log/trace evidence by `tool_use_id`,
- distinguish success, failure, rejection, and unknown,
- integrate `claude_code.skill_activated` into production,
- merge Skill tool and request-level `skill.name` usage,
- classify MCP server/tool metadata provider-neutrally,
- derive opaque artifact keys only from successful structured write evidence,
- test repeated writes, multi-file edits, failed edits, and Bash writes,
- keep FileChanged and snapshots corroborating rather than causal,
- verify commit attribution excludes read-only and failed-write runs.

Exit criteria:

- counts and outcomes match native evidence exactly,
- skill/MCP/subagent usage does not disappear or duplicate,
- files contain only causally proven relative paths,
- privacy canaries are absent everywhere.

### Phase 7: Real-time delivery and recovery

Tasks:

- measure source emission, Tirion acceptance, queue, HTTP attempt, receiver, and
  final correction separately,
- verify lifecycle-first priority under concurrent runs,
- keep the fixed three-second first-terminal deadline,
- trigger query-family completion independently from global quiet/rebuild work,
- when late exact query/session evidence began on or before a retained explicit
  completion boundary, wake that query-family correction independently from
  unrelated global quiet-window traffic while keeping post-boundary evidence
  ineligible,
- after an exact Claude Stop/closed-root join, admit and durably queue terminal
  meaning without awaiting outbound HTTP or an in-flight same-run update; keep
  only HTTP attempts serialized in lifecycle order,
- retain the implemented and deterministically verified priority for a fresh due
  trusted Claude closed-root terminal ahead of fresh `settling` snapshots from
  the same or another run only when its webhook
  `codingHarness`/runtime are `claude-code`, `evidence.basis` is `root_span`,
  and `evidence.delayed` is `false`; do not preempt an update attempt already in
  flight, keep the settling snapshot as a fallback when terminal delivery fails
  or retries, and suppress it only after terminal delivery succeeds; preserve
  normal completed lifecycle order for every other terminal,
- verify per-run serialization and strict ordering,
- stress receiver downtime, timeout, retry, restart, backlog, historical replay,
  workspace scans, and commit reconciliation,
- keep semantic terminal authority monotonic across alternating projections.

Exit criteria:

- starts and terminals meet latency targets,
- one run cannot block another behind enrichment,
- retries survive restart without duplicates or ordering loss,
- durable queue and blocked counts return to zero.

### Phase 8: Freeze regression coverage and close the spike

Tasks:

- add redacted native parser fixtures for the minimum and current supported
  Claude versions,
- add Claude scenarios to the complex safe-observation fixture matrix,
- add deterministic usage and webhook tests,
- extend the local Claude lifecycle system harness,
- add pinned native Claude runs to an opt-in nightly/release matrix,
- update `constructs.md`, webhook docs, harness monitoring docs, and the spike
  outcome report,
- run `npm run check` and the full live matrix,
- review the diff for hidden provider coupling and privacy regressions.

Exit criteria:

- every discovered defect has a deterministic regression,
- deterministic tests run in CI,
- native tests record exact provider version and are release-gated,
- no unresolved high-integrity defect is described as complete.

## Scenario Matrix

| ID | Scenario | Primary invariant |
| --- | --- | --- |
| `CC-00` | read-only root | one lifecycle, no files, exact root usage |
| `CC-01` | one successful Edit/Write | one causal relative file and successful tool |
| `CC-02` | failed write | failure count increments, no causal file |
| `CC-03` | rejected tool | rejection is not execution success or failure |
| `CC-04` | deliberate non-zero Bash | structured failure if exported; otherwise honest unknown |
| `CC-05` | one Explore/custom subagent | one exact child folded once |
| `CC-06` | three same-name parallel children | complete group only with three exact links |
| `CC-07` | four same-name children plus two files | exact child aggregate, root residual, and two causal files |
| `CC-08` | standard subagent attempts nested delegation | no invented grandchild or nested ownership; recursive support requires a separate team/workflow scenario |
| `CC-09` | missing SubagentStop | usage lineage may be complete while outcome remains unknown |
| `CC-10` | SubagentStop blocked then allowed | first attempt cannot finalize child |
| `CC-11` | Stop blocked then allowed | first attempt cannot finalize root |
| `CC-12` | Stop with background subagent/shell | run remains open or settling until background work completes |
| `CC-13` | StopFailure/API terminal error | failed terminal, no false completion success |
| `CC-14` | user interrupt | explicit non-success fallback and bounded lifecycle behavior |
| `CC-15` | skill exists but unused | zero skill activity |
| `CC-16` | direct Skill tool invocation | one skill activity and exact attributed requests |
| `CC-17` | slash/nested skill | trigger and usage attribution without duplicate Skill rows |
| `CC-18` | MCP success/failure | exact server/tool, call ID, outcome, and usage |
| `CC-19` | repeated writes to one file | one unique file, multiple tool calls |
| `CC-20` | Bash creates a file | no path claim without structured causal evidence |
| `CC-21` | two watched repositories in one OTLP batch | each record routes exactly or fails closed |
| `CC-22` | worktree child | correct watched repo/worktree identity and relative files |
| `CC-23` | delayed/duplicated/out-of-order OTLP | one semantic run and stable versions |
| `CC-24` | restart before terminal delivery | durable recovery and one ordered terminal |
| `CC-25` | receiver timeout/retry | bounded attempt and idempotent replay |
| `CC-26` | concurrent fresh run plus historical backlog | fresh lifecycle preempts stale work |
| `CC-27` | workflow/team run on supported version | exact workflow grouping and nested usage |
| `CC-28` | auxiliary/compaction request | no separate public run; explicit overhead policy |

## Next-Release Use-Case Register (2026-07-16)

This register deduplicates every unresolved, unaccepted, or intentionally
unsupported Claude Code item recorded in this program and its outcome report.
It distinguishes deferred work from completed scope: bounded local-MCP success
and controlled failure are complete, while third-party MCP remains separate.
Every newly accepted surface must retain the existing privacy-canary and
content-gate checks.

| Cluster | Concrete remaining use cases | Current posture and required evidence |
| --- | --- | --- |
| **P0: lengthy cross-harness benchmarks** | Repeated current-binary Claude Code and Codex runs; isolated receiver ingestion; sustained update cadence; provider-to-Tirion-to-receiver phase clocks; concurrent long roots, slow receiver, backlog, retry/restart/replay, and final conservation at high activity volume; exact semantic handling of layered Codex `Bash`/`exec` activity | `REL-E2E-20260716T082716Z` accepts one concurrent functional baseline. The next release must turn that single sample into a repeatable benchmark suite with p50/p95 reporting and receiver/dashboard contention removed from the ingestion path. |
| **Deferred: native late decision** | CC15D/CC15C topology and exact rejected-Write correction; live source-pruning of file/commit claims; restart-spanning correction boundary | Explicitly deferred by the operator. If resumed, CC15D must first be eligible; a later CC15C pass still requires real `TelemetryIngress` and receiver delivery. |
| **Direct-child terminal topology** | CC-09 missing `SubagentStop`; CC-10 blocked-then-allowed `SubagentStop`; terminalized four-child CC-07; child-first split envelopes; join restart/eviction | The bounded three-child core is accepted. Each remaining ordering/closure shape needs its own exact native child/parent evidence. |
| **Teams, workflows, recursive lineage** | CC-27 `workflow.run_id`, team members, recursive links, ambiguous-link failures | No production workflow/team contract is accepted. Ordinary nested subagents remain an intentionally unsupported negative control. |
| **Skills** | Request-level `skill.name`, skill-attributed token ownership, CC-17 slash/nested-skill deduplication | Native skill absence/presence is known; production usage attribution is not accepted. |
| **Tool and file authority** | CC-02 failed write, CC-19 repeated writes, CC-20 Bash-created file, broader tool outcome/call-ID integrity | The exercised structured-write paths are accepted. New tool/file shapes need exact structured causal proof; Bash/timing alone cannot claim a file. |
| **Repository routing** | CC-21 multiple watched repositories in one batch; CC-22 worktree identity; ambiguous repository binding | Must route each safe record to exactly one watched repository or fail closed. |
| **Delivery, recovery, and latency** | CC-23 delayed/duplicate/out-of-order OTLP; CC-24 restart before terminal delivery; CC-25 timeout/retry; CC-26 fresh work versus backlog; retry exhaustion; loaded terminal SLO and release reporting | The narrow cross-run bypass is accepted. These broader durability, ordering, recovery, and latency surfaces require isolated live evidence. |
| **Interruptions and failures** | Structured user interruption/rejection source; retry exhaustion; native failure categories beyond `authentication_failed`; prompt-to-`StopFailure` restart | The conservative open-root fallback is accepted. No missing terminal may be promoted to a normal completion. |
| **Interactive and version coverage** | Interactive metadata census beyond accepted CC-04I-R/CC-06-RRR slices; cross-version native matrix; dynamic content-gate effectiveness; other authentication providers | Each interactive/provider-version contract needs independent identity, ordering, and privacy evidence. |
| **Usage, auxiliary work, and billing** | Full root/child/activity token conservation; compaction and other auxiliary request categories; subscription/direct API/Bedrock/Vertex billing contexts | Exact session-title exclusion is accepted. Other auxiliary work remains counted or unclassified until exact native purpose evidence exists. |
| **Third-party MCP** | Approved authenticated MCP server, OAuth/connector semantics, MCP usage attribution | Out of the accepted local CC18 transport boundary; requires a separate privacy-reviewed plan and authorization. |

### Immediate non-CC15 live acceptance — `CCLIVEA-20260716T073022Z` (accepted)

The print-mode isolated E2E harness
`local-harnesses/tirion_local_server_dual_provider_lifecycle_test.sh` remains a
broad regression asset, but its current Claude-only attempt produced no
lifecycle event after its first real call and was stopped before the long
receiver timeout. It is not acceptance evidence and should not be retried as a
substitute for human-typed interactive provenance.

The accepted immediate run was a fresh normal interactive Claude session in the
watched `tirion_local_log_server` repository. Its safe local projection retained
exactly one completed repository-attributed Claude Code root, one `Read`
activity with zero failures and zero unknown outcomes, complete provider-reported
usage coverage, and no prompt storage or sensitive audit evidence. The safe
diagnostic window contained no error-severity event.

This validates managed source readiness, user-submission ingress, root lifecycle
assembly, one safe Read activity, and privacy-safe local run projection without
touching CC15. It predates the webhook-enabled release gate below and does
**not** close a next-release cluster by itself.

### Cross-harness webhook release acceptance — `REL-E2E-20260716T082716Z`

The installed normal agent observed concurrent read-only Claude Code and Codex
roots in the watched `tirion_local_log_server` repository and delivered both to
the existing authenticated loopback receiver. Claude ran for 349 seconds and
produced one start, 20 updates, and terminal versions 1 through 3. Codex ran for
520 seconds and produced one start, 43 updates, and terminal versions 1 and 2.
For both roots, receiver order was start, updates, then strictly increasing
terminal replacements; there were no duplicate deliveries, post-terminal
updates, blocked rows, or queued rows after drain.

The first receiver delivery followed source start by 2.334 seconds for Claude
and 2.582 seconds for Codex. The first terminal delivery followed the provider
completion boundary by 3.583 seconds and 4.945 seconds respectively. Both final
snapshots reported final usage coverage, complete activity coverage for the
reported surface, and exact activity-to-run token conservation. The privacy
audit found no prompt marker, absolute path, forbidden content field, or
unparseable payload in the acceptance window. One delayed usage-projection
Codex run was independently visible in the same window and remained separate
from the two prompt-hook benchmark roots.

This is the release-scoped functional acceptance for lengthy concurrent run
monitoring and webhook replacement semantics. It is not a statistical
performance claim: update-gap p50/p95, layered Codex `Bash`/`exec` semantics,
slow-sink capacity, restart/replay under load, and isolated receiver ingestion
remain P0 next-release work. The workspace-built drain barrier reached a full
ingress/runtime/webhook fixed point on retry and the agent restarted healthy;
the older installed CLI did not yet expose that command, so packaged CLI/runtime
version parity remains a pre-tag distribution smoke requirement.

## Representative Native Prompts

These are synthetic test prompts. Adjust them only when the native harness cannot
produce the intended shape, and record the exact submitted prompt outside
Tirion's product data.

### Read-only control

```text
Inspect this disposable repository using Read, Glob, and Grep. Do not modify any
files. Return a short summary, then stop.
```

### One child

```text
Launch exactly one Explore subagent to inspect the test layout. Wait for it to
finish, verify one finding yourself with a read-only tool, then stop. Do not edit
files.
```

### Three same-name children

```text
Launch exactly three Explore subagents in parallel. Assign one to source layout,
one to tests, and one to configuration. Wait for all three. Run one read-only
shell command in the root, summarize their findings, then stop. Do not edit
files.
```

### Four children and multi-file writes

```text
Launch exactly four Explore subagents in parallel and wait for all four. Give
each a distinct read-only repository question and ask each to run one read-only
shell command. In the root, create fixtures/claude-four-child-a.txt and
fixtures/claude-four-child-b.txt using structured file-edit tools, then update
both once more with a second structured edit. Run one successful and one
deliberately failing read-only shell command. Verify the two final files and stop.
```

### Skill control and invocation

```text
A disposable skill named tirion-claude-integrity-probe exists. Do not invoke any
skill. Inspect README.md and stop.
```

```text
Invoke the tirion-claude-integrity-probe skill exactly once, follow its
instructions, then stop.
```

The harness must independently verify what actually happened. Prompt intent is
not evidence that the expected children, tools, or files occurred.

## Acceptance Invariants

### Lifecycle and identity

- exactly one `run.start`,
- at least one meaningful `run.update`,
- at least one `run.ended`,
- no update after the first terminal,
- stable `runId`, repository, `startedAt`, harness, and runtime,
- contiguous terminal versions beginning at 1,
- unique event IDs and idempotency keys,
- no duplicate terminal meaning after removing event ID and version,
- no standalone public lifecycle for exactly linked children,
- no public lifecycle for internal or auxiliary processes.

### Usage and activity

- top-level total equals input plus output,
- every exported token dimension equals the selected native ledger,
- root plus exact children equals the public run,
- activity rows conserve each top-level token dimension,
- one native request appears once,
- one native tool call appears once,
- one skill invocation appears once,
- failure plus unknown never exceeds count,
- exact child count is required for complete same-name attribution,
- unallocated usage remains explicit,
- model/provider and billing context stay distinct.

### Files and attribution

- read-only runs have no files,
- successful structured writes expose only proven relative files,
- failed/rejected writes expose no file claim,
- repeated writes do not duplicate the file list,
- snapshot-only changes do not become run files,
- commit attribution links only causally contributing runs,
- multi-repo and worktree routes never cross-contaminate.

### Privacy

- no prompt or response canary in database, log, diagnostic, fixture, or receiver,
- no tool argument/output or command canary,
- no file-content or diff canary,
- no transcript path,
- no absolute repository path,
- no raw OTLP or hook payload,
- no sensitive Stop/background task descriptions,
- only allowlisted relative paths cross the webhook boundary.

### Delivery and durability

- lifecycle ordering is about delivered snapshots, not every queue row: normal
  completed delivery is `run.start -> delivered run.update(s) -> run.ended`,
  with the narrow trusted Claude closed-root exception recorded below; successful
  terminal delivery suppresses pending stale running updates,
- per-run coalescing and serialization under concurrency,
- an eligible terminal has a durable outbox row even when a same-run update
  attempt is in flight; attempts stay serialized. Only a due terminal whose
  webhook `codingHarness`/runtime are `claude-code` and `evidence.basis` is
  `root_span` with `evidence.delayed: false` has fresh due selection precedence
  after starts over fresh settling updates from the same or another run in
  ordinary and bounded-bypass lifecycle selection; failed/retrying delivery
  retains the same-run settling fallback, and no stale update follows successful
  terminal delivery,
- delivered update high-water prevents state, token, activity, or coverage
  regression except an explicitly proven source-pruning correction,
- distinct running-update meaning receives a unique content-derived event ID;
  equivalent meaning reuses its ID and superseded unpublished rows stay stale;
  distinct delivered meanings also receive strictly increasing logical
  `updatedAt` values even when provider source time is equal,
- terminal lifecycle priority over enrichment and historical work,
- a fresh lifecycle anchor can use only the bounded bypass lane while one
  unrelated ordinary delivery is in flight; ordinary deliveries remain
  serialized,
- bounded HTTP attempts and durable retry,
- restart recovery without duplicate meaning,
- zero queued and blocked rows after successful completion,
- later correction only when semantic authority improves,
- semantic child/file/outcome authority never downgrades.

## Latency Measurement And Targets

Record these timestamps independently:

- `T_submit`: test harness submits the prompt,
- `T_hook_accept`: Tirion accepts UserPromptSubmit,
- `T_start_attempt`: Tirion begins the start webhook HTTP attempt,
- `T_start_receive`: receiver records `run.start`,
- `T_source_terminal`: trusted provider source terminal boundary when retained,
- `T_stop_accept`: Tirion accepts final completion evidence,
- `T_interaction_close`: Tirion accepts the closed authoritative interaction,
- `T_terminal_queue`: Tirion durably queues first terminal meaning,
- `T_terminal_attempt`: Tirion begins the first terminal HTTP attempt,
- `T_terminal_receive`: receiver records the first `run.ended`,
- `T_final_receive`: receiver records the final authoritative terminal version.

Report separate latency components:

- provider source delay: `T_hook_accept - T_submit`,
- provider terminal emission: `T_interaction_close - T_source_terminal` when
  the trusted source boundary is retained,
- Tirion start delay: `T_start_receive - T_hook_accept`,
- Tirion terminal admission: `T_terminal_queue - T_interaction_close`,
- same-run dispatch wait: `T_terminal_attempt - T_terminal_queue`,
- first terminal end-to-end delay: `T_terminal_receive - T_source_terminal`,
- final truth end-to-end delay: `T_final_receive - T_source_terminal` when the
  trusted source boundary is retained,
- queue latency and observation latency from Tirion diagnostics.

Initial program targets:

- Tirion acceptance-to-receiver `run.start` p95 <= 500 ms on loopback,
- live update acceptance-to-attempt p95 <= 750 ms outside receiver contention,
- first terminal <= 5 seconds after the trusted source terminal boundary when it
  is retained,
- target first terminal near the fixed 3-second deadline when required authority
  reaches the live terminal path before that deadline,
- final grouped terminal <= 5 seconds when all native evidence arrived before
  the deadline,
- otherwise an honest non-final terminal followed by a bounded correction within
  the existing 15-second horizon; a boundary-complete first snapshot may retain
  `final` coverage, but any later eligible pre-boundary evidence must still
  publish a bounded higher replacement version,
- a late provider closed-root emission is an attributed end-to-end SLO failure;
  it must not relax Claude's exact Stop/closed-root authority rule or be hidden
  by moving the deadline to Tirion receipt time,
- no latency benchmark may combine provider emission delay, Tirion queue delay,
  and receiver event-loop delay into one unexplained number.

Use a receiver process separate from dashboard indexing for p95 measurements.

## Regression Architecture

Build five layers of protection:

1. **Native schema fixtures**
   - redacted/synthetic Claude hook and OTLP JSON,
   - version-tagged,
   - parser and privacy assertions,
   - minimum-supported and current-supported Claude versions.
2. **Safe-observation scenario fixtures**
   - root, child, source-supported nested child, skills, tools, usage, outcomes,
     artifacts,
   - no raw content,
   - provider-specific evidence mapped into provider-neutral atoms.
3. **Engine contract tests**
   - authority selection,
   - request deduplication,
   - recursive folding,
   - token and outcome conservation,
   - internal exclusion.
4. **Webhook and storage tests**
   - ordering,
   - terminal versions,
   - files,
   - privacy,
   - retry/restart/priority,
   - bounded query-family reads.
5. **Pinned native system matrix**
   - real Claude CLI runs in disposable repos,
   - exact version and model recorded,
   - opt-in nightly and release gate,
   - provider ledger compared to receiver ledger.

Deterministic layers belong in the normal CI gate. Native model-driven runs
belong in a separately reported pinned matrix because model behavior and network
availability are not deterministic.

## Diagnostics Required For The Spike

Add or verify privacy-safe diagnostics for:

- source configuration and enhanced trace gate state,
- first hook receipt per run,
- first OTLP receipt per signal,
- prompt-hook to prompt-ID reconciliation,
- completion candidate and final authority selection,
- active background task count without descriptions/commands,
- unresolved in-memory background-root count at Claude `prompt_input_exit`,
  including its explicit diagnostic-only/no-terminal disposition,
- child link accepted/rejected reason,
- request deduplication authority,
- unallocated usage reason,
- query-family projection duration and queue latency,
- first terminal deadline and correction horizon,
- webhook queue/attempt/receiver timing,
- repository route missing/conflict,
- causal artifact accepted/dropped reason,
- internal-session exclusion,
- source schema/version mismatch.

Diagnostics must explain reduced coverage without exposing raw provider content.
Live acceptance audits must request diagnostics with the run's inclusive UTC
`since`/`until` bounds. The latest-only diagnostic snapshot is intentionally
capped and may be displaced within minutes by concurrent harness telemetry; the
windowed log surface retains the audit slice within the existing active-plus-
three-rotation private log bound and still returns at most 1,000 safe events.

## Debugging Decision Tree

| Symptom | Inspect first | Owning construct |
| --- | --- | --- |
| no `run.start` | hook config, enhanced gate, hook receipt, repo route | Source Configuration / Ingress |
| two public roots | hook-to-prompt-ID reconciliation, internal marker | Correlation Ledger |
| run ends before child/background work | blocked Stop, background tasks, interaction close | Classification / Usage Projection |
| background-child root stays open after exit | background Stop, continuation floor, correlation-only task notification, eligible final Stop/closed-root join, and count-only `prompt_input_exit` diagnostic | Classification / Ingress |
| child published separately | `agent_id`, parent span, Agent tool call linkage | Correlation / Usage Projection |
| missing child tokens | request `agent_id`, authority selection, child fold | Classification / Usage Projection |
| inflated tokens | duplicate log/trace request identity or retries | Usage Projection |
| false tool success or rejection | tool result, failure hook, exact permission decision identity | Classification |
| skill count/usage missing | skill event, Skill tool, request `skill.name` | Classification / Usage Projection |
| file missing | successful structured write activity and artifact key | Ingress / Attribution |
| unrelated file included | snapshot-only or Bash/timing heuristic | Attribution |
| terminal late | source-boundary to closed-root emission, terminal-admission lane, in-flight update claim, same-run settling/terminal selection priority | Runtime / Dispatch / Storage |
| event queued forever | durable deadline, retry timer, HTTP timeout | Dispatch / Storage |
| sensitive canary found | raw ingress retention or outbound allowlist | PrivacyGuard |

## Prioritized Engineering Backlog

### P0

- **Completed for the accepted root slice:** manage and verify the enhanced
  telemetry trace gate through deterministic, native, managed full-stack, and
  repaired persistent-user evidence,
- **Completed for the implemented 2.1.207 slice:** build the metadata-only native
  Claude census and versioned request-purpose/Stop fixtures,
- **Completed for the accepted non-interactive root slice:** correct
  Stop/background/blocked-stop completion semantics,
- **`authentication_failed` complete; broader categories pending:** add
  `StopFailure` handling,
- **Completed for every exercised source and full-stack boundary:** preserve
  privacy under content-bearing hook payloads; continue the same gate for each
  newly accepted surface.

### P1

- **CC07R/CC07RR integrity/privacy passed; terminal SLO still failed:** retain
  the implemented dedicated terminal-admission lane, immediate Claude
  closed-root query projection, and durable terminal queueing during a same-run
  update attempt; retain the implemented, deterministically verified precedence
  only for the fresh trusted Claude closed-root terminal
  (`codingHarness`/runtime `claude-code`, `evidence.basis: "root_span"`,
  `evidence.delayed: false`) over fresh settling rows from the same or another
  run, without preempting an in-flight update or discarding the same-run fallback
  after a failed/retrying terminal attempt. All other completed lifecycles retain
  their normal delivered order. A rebuilt live rerun remains pending to meet the
  source-boundary deadline without weakening per-run send order,
- **Bounded direct-parallel core and rebuilt clock/window check passed; targeted
  batch/bypass and broader lineage pending:** exact Agent response/start identity
  and parent-first `tool_use_id` ownership; the next live run must exercise one
  Agent tool batch and an unrelated in-flight delivery; recursive and nested
  `agent_id` / `parent_agent_id` lineage remains open,
- **Accepted for exact session-title requests in the three-root full-stack
  run; broader conservation pending:** request log/trace deduplication and full
  token conservation,
- **Native `CC-15`/`CC-16` activation evidence complete; production attribution
  pending:** skill activation plus skill-attributed usage,
- **CC07Y one-child background-root gap reproduced:** keep the exact
  Stop/closed-root authority unchanged and emit only the bounded count-only
  `claude_background_root_missing_terminal` diagnostic on `prompt_input_exit`
  when an unresolved in-memory background root remains. Prove it with a fresh
  one-child exit and controls that show no durable observation, terminal, or
  webhook is fabricated,
- tool outcome and call-ID integrity,
- **Accepted for the two exercised writing roots:** causal files and writer-only
  commit projection for structured write tools,
- Claude complex-run safe fixtures and webhook replays,
- a targeted rebuilt three-child rerun that actually emits one Agent tool batch
  and forces a fresh lifecycle anchor behind an unrelated in-flight delivery;
  `CC06B-20260713T060835Z` already live-verified strict update ordering and
  windowed diagnostics, but not those two conditions; then four-child
  acceptance, including exact same-name conservation and no duplicate/open-launch
  distortion,
- **CC18 local-MCP transport accepted:** the disposable capability-gated fixture
  passed both one successful and one controlled-failure call through the normal
  Tirion route, each with exactly one canonical accepted MCP observation and no
  duplicate, rejected, or unknown outcome. This remains deliberately separate
  from cloud OAuth, personal-data connectors, third-party MCP semantics, or MCP
  usage attribution,
- **CC15D must precede any CC15C retry:** the prior TTY denial reached only the
  opaque `native_decision_not_held_closed_root_trace_count` gate. The new
  source-only interactive topology census uses no Tirion agent, webhook, or
  decision release; it must first classify the exact controlled hook/decision/
  closed-root topology with fixed metadata-only categories. Only an
  `cc15cEligibility: "eligible"` census result can reopen—not pass—CC15C for
  separate review. **CC15C harness self-tested; live acceptance still pending:**
  its separately authenticated disposable-profile relay probe must retain a
  real persisted transcript for exactly one `UserPromptSubmit`, one forwarded
  Stop, and one exact closed-root trace joined to that session; it must return
  one controlled PreToolUse Write denial and hold only the native rejection
  carrying the same opaque `tool_use_id`. It requires no StopFailure,
  PostToolUse, or PostToolUseFailure, a pre-release non-rejected root-span
  terminal, and a later exact rejected-Write terminal. After release it must
  seal the relay, obtain the direct agent's bounded pre-stop ingress/runtime/
  webhook fixed point, seal the receiver, and keep both sealed through the
  quiet interval and direct-agent exit. It must leave no prompt marker in
  Tirion-controlled state, keep the disposable Git HEAD unchanged, and cannot
  convert the denied Write into file or commit evidence.

  The first live rerun after restoring the native session attribute cleared the
  relay-held decision gate but timed out at the receiver baseline-terminal
  gate. Its fixed receiver result was `no_run_received`: no public lifecycle
  reached the receiver. This is inconclusive rather than a live acceptance;
  relay correctness alone does not prove transcript admission, terminal
  authority, or webhook delivery. No private profile, transcript, payload,
  identity, or local log was inspected. That direct-agent reduction returned
  `provenance_unavailable`, proving only that the trusted submission authority
  remained unresolved. The next rerun narrowed that to
  `provenance_origin_not_human_typed`: the bounded transcript read and candidate
  match succeeded, but its final exact origin/prompt-source pair did not meet
  the human/typed authority rule. The following source-shape rerun returned
  `provenance_transcript_origin_kind_missing`. Its prompt was explicitly piped
  to `claude -p`, which the local CLI defines as non-interactive. That outcome
  is therefore the correct fail-closed negative control, not a missing alias or
  a retry/timing defect. Do not add speculative aliases or relax authority.
  Use the new TTY-only interactive mode for the next native interactive
  provenance attempt; it proves neither a human identity nor authority by
  itself, because the production rule remains the exact Claude
  `origin.kind=human` and typed prompt-source pair.

  The first TTY attempt did exercise the controlled denial, but the relay then
  returned `native_decision_not_held_closed_root_trace_count`. This is an exact
  fail-closed harness condition, not a production provenance defect: it says
  only that the session-matched closed-root count was not exactly one, before
  the decision-time-boundary check. Do not run another CC15C prompt to classify
  that opaque count. Run CC15D's dedicated metadata-only interactive
  trace-topology census first; it records only cardinality/relationship enums
  and cannot create a Tirion fact, lifecycle, file, commit, or correction.
  Third-party authenticated MCP remains a separately reviewed later scope.

#### CC15D interactive source-topology census

CC15D is a source-shape census, not an acceptance rerun. It launches Claude in
a user-authorized TTY with a private authenticated profile, a disposable empty
workspace, one controlled Write denial, and a loopback in-memory receiver. It
does not start Tirion, configure a webhook, inspect a transcript, retain a raw
payload, release a native decision, or write a production observation. The
receiver reduces native hooks, logs, and traces to fixed cardinalities and
opaque identity relations before clearing request buffers.

The harness itself always creates its disposable workspace and receiver files
under `/tmp`, never an inherited `$TMPDIR`. Before final output it seals the
authenticated loopback ingress, keeps the receiver live for a bounded two-second
post-seal soak, and reads one final sealed status; a late authenticated Claude
export is therefore counted as `postSeal` rather than disappearing at a closed
port. Invalid or unauthenticated local traffic is not provider ingress evidence.
The expected session, controlled PreToolUse, and Stop must each equal the unique
submission session, and the native decision must equal that same submission
session; the decision must also lie in one unique root for it. Prompt-ID alias
variance remains an exposed diagnostic only and cannot determine eligibility.

Create and authenticate a dedicated private profile yourself. Reuse an existing
valid `tw-cc15c-claude.*` profile only if it remains disposable and passes the
same strict isolation guard; otherwise create a new `tw-cc15d-claude.*` root.

```sh
PRIVATE_ROOT="$(mktemp -d /tmp/tw-cc15d-claude.XXXXXX)"
chmod 700 "$PRIVATE_ROOT"
CLAUDE_CONFIG_DIR="$PRIVATE_ROOT" claude auth login --claudeai
find -P "$PRIVATE_ROOT" -type d -exec chmod 700 {} +

env -u CLAUDE_CONFIG_DIR \
  TIRION_CC15D_CLAUDE_CONFIG_DIR="$PRIVATE_ROOT" \
  TIRION_CC15D_CLAUDE_MODEL=claude-sonnet-5 \
  ./local-harnesses/claude_cc15d_interactive_topology_census.sh
```

When Claude opens, submit exactly this prompt, wait for the controlled denial,
then type `/exit`:

```text
CC15D-<valid UTC timestamp>

Use exactly one Write tool call to attempt to create cc15d-denied.txt containing one short non-secret line. Do not use any other tool. Do not repeat this marker: CC15D_TIRION_PRIVACY_CANARY. After the Write attempt, stop.
```

Report only the final one-line JSON census result, never Claude's transcript or
reply. `cc15cEligibility: "eligible"` establishes only the strict native source
topology needed to reconsider CC15C: one submission, controlled PreToolUse
denial, matching rejected Write decision, the expected session and Stop joined
to that same submission/decision session, a valid unique root, and a decision
inside that root boundary. It is not transcript provenance, Tirion
ingress, receiver, file, commit, usage, or correction acceptance. Any other
fixed-enum result is evidence to record, not a reason to retry or relax CC15C.

#### CC15C opt-in live command

This is deliberately a user-authorized model invocation. It does not reuse,
copy, or link the normal Claude profile. The harness accepts only the fresh
`mktemp`-style root shown below: it must be mode `0700`, non-symlinked, outside
`$HOME` and the active normal Claude root, and neither may contain the other.
It snapshots metadata (never contents) for the normal Claude profile surfaces
before and after the run. First create that private profile and perform the
provider login yourself; the profile retains the synthetic Claude transcript
required for provenance and must be deleted by the operator only after
reviewing the result.

Claude may create nested profile directories using the ordinary shell umask,
so normalize only those directory modes after login. The process-local
`env -u` below prevents an inherited normal-profile selection from aliasing
the private root without changing the operator's shell configuration. Use
`/tmp` rather than an arbitrary `$TMPDIR`, which could be configured under
`$HOME` and violate the required disjointness.

The normal-profile alias check follows symlinked directories only through a
bounded, cycle-safe metadata walk. It accepts an unrelated link surface but
still rejects both direct and indirect links to the private root; it neither
retains nor exposes normal-profile file contents or path values. This replaces
a generic nested-link rejection found during preflight-only CC15C preparation,
not a model-invocation result.

```sh
PRIVATE_ROOT="$(mktemp -d /tmp/tw-cc15c-claude.XXXXXX)"
chmod 700 "$PRIVATE_ROOT"
CLAUDE_CONFIG_DIR="$PRIVATE_ROOT" claude auth login --claudeai
find -P "$PRIVATE_ROOT" -type d -exec chmod 700 {} +

env -u CLAUDE_CONFIG_DIR \
  TIRION_CC15C_CLAUDE_CONFIG_DIR="$PRIVATE_ROOT" \
  TIRION_CC15C_CLAUDE_MODEL=<exact-immutable-model-id> \
  TIRION_CC15C_PROMPT_MODE=interactive \
  TIRION_CC15C_RELAY_HOLD_MS=300000 \
  ./local-harnesses/claude_cc15c_late_decision_acceptance_test.sh
```

The model value must be an explicit `claude-...` identifier, not a moving
alias. When Claude opens, type exactly this one prompt, wait for its one Write
attempt to finish, then type `/exit`:

```text
Use exactly one Write tool call to attempt to create cc15c-denied.txt containing one short non-secret line. Do not use any other tool. Do not repeat this marker: CC15C_TIRION_PRIVACY_CANARY. After the Write attempt, stop.
```

Do not send Claude's transcript or reply to Tirion; report only the final
`CC15C ...` result. Interactive Claude supports neither `--max-budget-usd` nor
the print-mode turn cap, so the invocation deliberately has no CLI-enforced
spend cap; the `Write`-only tool set, one-turn protocol, and five-minute relay
hold bound the probe operationally. An invalid-profile preflight returns only
a fixed category suffix (never a profile path, symlink target, credential, or
transcript detail). The harness otherwise prints only a bounded safe summary.
A pass means the real native decision reached `TelemetryIngress` after the
baseline terminal and the receiver observed the required root-span
rejected-Write correction matched to the controlled denial, with sealed
relay/receiver counters still clean after the direct agent's fixed-point
pre-stop drain and orderly exit; an absent terminal, expired hold, missing
transcript or closed-root authority, duplicate hook, completion hook, changed
Git HEAD, late post-seal ingress/delivery, or any extra file/commit/execution
claim is a failed/inconclusive probe, not an acceptance. Local control calls
explicitly bypass ambient proxy settings. It intentionally does not establish
live file/commit retraction. After review, remove the operator's private profile
with `rm -rf "$PRIVATE_ROOT"`.

#### CC18 capability-gated local MCP transport probe

Do not authenticate Gmail, Google Calendar, or Google Drive for this probe.
They are personal-data connectors and would add account scope without proving a
better Tirion contract. Instead, the disposable local fixture exposes exactly
one fixed no-argument, read-only tool for one interactive session. It has no
network, filesystem, account, or credential access. A fresh capability is passed
only from its generated MCP configuration to the child server; it is never
shown to Claude or retained in the fixture audit. The audit contains only fixed
server/tool labels, counters, and a negotiated protocol-version token.

Run this from the ordinary watched repository while the normal Tirion receiver
is ready. Set `TIRIONWATCH_ROOT` to this repository's absolute path; omit the
model variable to use the normal Claude default.

```sh
cd /path/to/watched/repository
TIRIONWATCH_ROOT=/path/to/TirionWatch \
TIRION_CC18_CLAUDE_MODEL=claude-sonnet-5 \
  "$TIRIONWATCH_ROOT/local-harnesses/claude_cc18_local_mcp_test.sh" success
```

When Claude opens, submit exactly this prompt, wait for its response, then type
`/exit` so the launcher can validate its metadata-only fixture audit:

```text
CC18A-<UTC timestamp>

Use exactly one MCP tool call: mcp__tirion_cc18_local__tirion_cc18_readonly_success.
Use no other tool and do not inspect files, environment variables, or configuration.
After the MCP call returns, reply exactly:
CC18A local MCP success complete
```

Report only the final `CC18 local MCP success fixture complete` line. The
launcher accepts only one initialized local fixture, one or more protocol tool
lists, exactly one expected no-argument tool call, no unexpected call, no
malformed protocol message, and a syntactically bounded negotiated version. It
does not claim a Tirion acceptance by itself.

`CC18A` completed the fixture but did **not** meet the next diagnostic gate:
the bounded normal Tirion window received Claude log/trace envelopes and
reported zero MCP activity atoms after its drain. Its normal query/execution
evidence did not classify the local action as MCP. That is a useful negative
result, not evidence that Claude emitted no MCP metadata and not a reason to
infer an MCP activity. Do not run the controlled-failure sibling yet.

#### CC18C isolated native-MCP metadata census

Run the following short, interactive source census next. It creates a
mode-0700 disposable workspace, uses only the normal already-authenticated
Claude session for model access, and deletes its private metadata-only loopback
output on exit. It neither reads nor changes the normal Claude configuration,
credentials, watched repository, or authenticated cloud MCPs; Claude may retain
its ordinary session record under the normal profile, which this wrapper never
reads. The
temporary strict MCP configuration exposes only the fixed local fixture. The
temporary hook receiver admits only the one exact static `PreToolUse` tool name;
all tool arguments, parameters, responses, paths, and raw bodies remain
unprojected and are deleted with the disposable root.

```sh
cd /path/to/any/directory
TIRION_CC18_CLAUDE_MODEL=claude-sonnet-5 \
  /path/to/TirionWatch/local-harnesses/claude_cc18_native_mcp_census.sh
```

When Claude opens, submit exactly this prompt, wait for the response, then type
`/exit`:

```text
CC18C-<valid UTC timestamp>

Use exactly one MCP tool call: mcp__tirion_cc18_local__tirion_cc18_readonly_success.
Use no other tool and do not inspect files, environment variables, or configuration.
After the MCP call returns, reply exactly:
CC18C native MCP census complete
```

Report only the two final local lines: `CC18 local MCP success fixture complete`
and `CC18 native MCP census complete: ...`. Do not share the Claude transcript.
The latter contains only fixed categories and counts. A
`native_mcp_evidence=explicit_server_tool_pair` result identifies a safe native
server/tool shape; `namespaced_tool_name` identifies the bounded
`mcp__server__tool` shape; `none` means this source emitted neither. Independently,
`hook_mcp_evidence=qualified` requires one exact allowed PreToolUse and one
matching successful PostToolUse with the same aliased call identity.
`ambiguous`, unexpected-hook, duplicate, or failure counts are non-acceptance.
This is a schema census, not a normal-profile wiring acceptance or evidence of
OAuth, Gmail, Calendar, Drive, or third-party MCP semantics. Only a source shape
that Tirion can classify without content may advance to the controlled-failure
fixture.

#### CC18D normal-Tirion replay acceptance

`CC18C` resolved the remaining source-shape ambiguity: Claude emitted one
native bounded `mcp__server__tool` record and one qualified `PreToolUse` /
`PostToolUse` pair. The `CC18A` normal-Tirion loss was therefore traced to the
existing delayed-provenance replay boundary: while a `UserPromptSubmit`
transcript retry was pending, Tirion dropped a normal non-Agent `PostToolUse`
before it could join the resolved prompt. The repair permits only the exact
bounded MCP grammar through that short in-memory replay path. It retains no MCP
argument, result, or `agentId`; a successful and controlled-failure regression,
plus malformed-name negative coverage, are deterministic-gated.

After rebuilding and restarting the Tirion receiver from this worktree, run the
normal-profile success acceptance from the watched repository. This wrapper
uses the same disposable no-argument local MCP fixture, snapshots only the
already-safe Tirion `runs` and `logs` APIs in a mode-0700 temporary directory,
and deletes those snapshots on exit. It prints fixed count deltas only.

```sh
cd /path/to/watched/repository
TIRION_CC18_CLAUDE_MODEL=claude-sonnet-5 \
TIRION_CC18_TIRIONCTL=tirionctl \
  /path/to/TirionWatch/local-harnesses/claude_cc18_tirion_mcp_acceptance_test.sh success
```

When Claude opens, submit exactly this prompt, wait for the response, then type
`/exit`:

```text
CC18D-<valid UTC timestamp>

Use exactly one MCP tool call: mcp__tirion_cc18_local__tirion_cc18_readonly_success.
Use no other tool and do not inspect files, environment variables, or configuration.
After the MCP call returns, reply exactly:
CC18D Tirion MCP success complete
```

Accept only this final fixed line (all values must be exact):

```text
CC18 Tirion MCP success acceptance complete: mcp_delta=1 failure_delta=0 rejected_delta=0 unknown_delta=0 hook_acceptance_delta=1 hook_activity_delta=1 hook_node_delta=1
```

The wrapper also prints the fixture's own completion line. Do not report
transcripts or raw CLI/API output. Run the controlled-failure sibling only after
this success line is accepted.

`CC18D` passed with that exact line. The normal Tirion route now proves one
deduplicated successful MCP activity and execution node from one local
no-argument tool call, with no failure, rejection, or unknown outcome.

#### CC18E normal-Tirion controlled-failure acceptance

The same wrapper then ran the fixed local controlled-failure tool. Its final
count-only result was:

```text
CC18 Tirion MCP failure acceptance complete: mcp_delta=1 failure_delta=1 rejected_delta=0 unknown_delta=0 hook_acceptance_delta=1 hook_activity_delta=1 hook_node_delta=1
```

This accepts the bounded local-MCP transport surface: normal success and an
explicit controlled failure each project exactly one canonical `mcp` activity
and execution node through the normal Tirion route. The result retains no MCP
argument, response, raw identifier, path, credential, or cloud-MCP data. It
does not claim OAuth, third-party authenticated-MCP behavior, MCP usage
attribution, or broad MCP-schema support beyond the exact bounded namespaced
shape exercised here.

### P2

- supported team/workflow recursive lineage and ambiguous-link failure modes;
  standard nested-subagent delegation is documented as unsupported and retained
  only as a negative control,
- worktree and multi-repository stress,
- workflow/team support on the now version-eligible but untested 2.1.207 runtime,
- interruption labeling beyond the accepted conservative fallback and retry-exhaustion behavior,
- compaction and auxiliary categories other than exact session-title generation,
- restart-spanning native purpose and ordinary-Stop/closed-root joins,
- interactive-mode native census,
- cross-version native matrix,
- latency histogram and release SLO reporting.

## Explicit Non-Goals

- prompt or response analytics,
- transcript storage or parsing as a product data source,
- command/output scraping,
- heuristically claiming Bash file paths,
- making estimated cost look like billing truth,
- adding Claude-only public webhook fields without a shared contract decision,
- weakening privacy to make a stress scenario easier,
- native UI automation before stable CLI and hook contracts are accepted,
- supporting undocumented private Claude internals when public hooks/OTLP suffice.

## Release Definition Of Done

The `v0.1.6` source-preview release boundary is complete when:

1. the accepted Claude Code and Codex source, lifecycle, activity, usage,
   privacy, and webhook behaviors remain deterministic-gated,
2. the webhook-enabled current-binary live acceptance above passes with a
   drained queue and healthy restart,
3. `npm run check` and the macOS package build pass from the release worktree,
4. the changelog, public monitoring/webhook docs, release checklist, and this
   release outcome agree on supported and deferred scope,
5. package contents include every newly referenced fixture and harness, and
6. a clean-checkout/clean-host smoke confirms installed CLI/runtime version
   parity before the tag is published.

Deferred entries in the next-release register do not block this bounded release.

### Full Program Definition Of Done (future releases)

The broader Claude optimization program is complete only when:

1. source setup, status, restore, and version compatibility are accurate,
2. prompt start, blocked/paused completion, final completion, and failure
   completion are fixture-backed,
3. direct, parallel, same-name, and source-supported nested children fold
   exactly once,
4. tools, skills, MCP calls, outcomes, and token usage match native evidence,
5. all token dimensions conserve at root, child, and activity levels,
6. files are causal, relative, deduplicated, and privacy-safe,
7. starts and terminals meet measured latency targets under load and recovery,
8. retries and restarts preserve ordering and idempotency,
9. privacy canaries are absent from every durable and outbound surface,
10. every discovered defect has a deterministic regression,
11. the live pinned Claude matrix passes at the supported versions,
12. `npm run check` passes,
13. `constructs.md` and public monitoring/webhook documentation match reality,
14. the final report distinguishes proven support, unsupported evidence, and
    residual risk.

## Evidence Log Template

Add one row per native discovery or acceptance run.

| Date | Claude version | Scenario | Prompt anchor -> hook | Hook -> start | Final authority -> terminal | Root/children | Tokens | Tools/skills/MCP | Files | Privacy | Outcome / defect |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-07-16 | Claude Code 2.1.211; Codex CLI 0.142.0 | `REL-E2E-20260716T082716Z` concurrent lengthy-run webhook release gate | One non-delayed prompt-hook root per harness in the exact watched repository | First receiver start at +2.334 s Claude / +2.582 s Codex | Non-delayed Claude root-span terminal at +3.583 s; non-delayed Codex Stop terminal at +4.945 s | Two independent roots; no child claim | Claude 26,847 / Codex 1,283,167; final coverage and exact activity conservation | Claude 20 updates over 349 s; Codex 43 over 520 s; reported tool/LLM surfaces retained their native unknown/success metadata | No file claim; read-only benchmark | Zero prompt-marker, absolute-path, forbidden-key, malformed-payload, duplicate-event, or post-terminal-update findings; queue/blocked returned to zero | Release-scoped functional acceptance. Terminal versions were Claude v1-v3 and Codex v1-v2. Fixed-point drain passed on retry and the agent restarted healthy. Statistical p50/p95, slow-sink/restart load, isolated receiver ingestion, and layered Codex Bash/exec interpretation remain next-release P0. |
| 2026-07-15 | n/a (deterministic; no model invocation) | `CC-18` capability-gated local MCP fixture and parser regression | No Claude prompt or hook was sent | No live start | No live terminal | No live root/child claim | No provider usage/cost claim | One fixed no-argument success tool and one fixed controlled-failure tool passed local JSON-RPC self-tests; bounded `mcp__server__tool` parsing and explicit-metadata conflict fallback are deterministic-gated | No file access | The fixture retains only fixed server/tool labels, counters, and a safe protocol-version token in an ephemeral mode-0600 audit; self-test proves it does not retain an injected tool argument | Harness/parser readiness only. It proves neither live Claude telemetry nor OAuth, third-party MCP, account, or personal-data semantics. Run the interactive local success probe before its controlled-failure sibling. |
| 2026-07-15 | not retained | `CC18A` interactive local-MCP success fixture | The fixture audit completed exactly one fixed no-argument local MCP call | No accepted MCP activity start | No terminal claim | No accepted root/child claim | No usage/cost claim | The local MCP operation completed, while the bounded Tirion diagnostic window observed zero MCP activity atoms after drain; its query/execution evidence did not classify the local action as MCP | No file access | Only safe counters/diagnostics were read; no Claude transcript, raw payload, argument, output, path, credential, or cloud-MCP data was inspected | Negative source-observation result. The fixture proves its own stdio transport only; native metadata absence versus unsupported safe shape remains unresolved. Run the isolated CC18C metadata census before controlled failure. |
| 2026-07-16 | not retained | `CC18C` isolated native-MCP metadata census | One successful fixed local MCP call produced a qualified `PreToolUse` / `PostToolUse` pair | No normal-Tirion start claim | No terminal claim | No root/child claim | No usage/cost claim | One correlated native namespaced tool record, one namespaced-tool record, one MCP-connection record, one qualified pre/post hook pair, and no post-tool failure or unexpected-hook record | No file access | Only fixed categories and counts were reported; the disposable direct receiver deleted its metadata-only safe output and never read a Claude transcript, raw payload, argument, output, path, credential, or cloud-MCP data | Source shape accepted. It exposed the delayed-provenance non-Agent `PostToolUse` drop; the bounded replay repair and later CC18D/CC18E normal-route acceptances close this local transport case. |
| 2026-07-16 | not retained | `CC18D` normal-Tirion local-MCP success acceptance | One fixed no-argument local MCP success call completed through the managed normal profile | One safe hook activity and execution node were accepted | Normal root completion was observed by the count-only acceptance wrapper | No root/child topology claim beyond the one current local fixture run | No usage/cost claim | Exactly one canonical MCP row; `mcp_delta=1`, `failure_delta=0`, `rejected_delta=0`, and `unknown_delta=0`; exactly one accepted `hook_claude_code_tools` observation with one activity and one node | No file access | The wrapper printed only fixed count deltas and deleted its mode-0600 safe snapshots; no transcript, raw payload, identifier, argument, response, path, credential, or cloud-MCP data was exposed | Success path accepted. The prior deferred-provenance drop is repaired. Together with CC18E, it completes bounded local-MCP transport acceptance. |
| 2026-07-16 | not retained | `CC18E` normal-Tirion local-MCP controlled-failure acceptance | One fixed no-argument local MCP controlled-failure call completed through the managed normal profile | One safe hook activity and execution node were accepted | Normal root completion was observed by the count-only acceptance wrapper | No root/child topology claim beyond the one current local fixture run | No usage/cost claim | Exactly one canonical MCP row; `mcp_delta=1`, `failure_delta=1`, `rejected_delta=0`, and `unknown_delta=0`; exactly one accepted `hook_claude_code_tools` observation with one activity and one node | No file access | The wrapper printed only fixed count deltas and deleted its mode-0600 safe snapshots; no transcript, raw payload, identifier, argument, response, path, credential, or cloud-MCP data was exposed | Controlled failure accepted. With CC18D, bounded local-MCP success/failure transport is complete; cloud OAuth, third-party MCP, and usage attribution remain outside this acceptance. |
| 2026-07-16 | n/a (deterministic; no model invocation) | `CC15D` source-only interactive topology census | No Claude prompt was sent | No Tirion start | No Tirion terminal | No root/child claim | No usage/cost claim | Synthetic unique/zero/many root, wrong-session plus expected-root decoy, mismatched-tool, prompt-alias variance, missing/before/after decision-time, malformed-root, extra-PostTool, authenticated post-seal, unauthenticated local traffic, and privacy-canary envelopes are self-tested against the loopback source receiver | No file access | The receiver accepts raw bodies only transiently, exposes only fixed enum status, pins its disposable root to `/tmp`, and self-tests that synthetic identifiers, timestamps, and canaries never reach output | Harness readiness only. It neither reads a transcript nor starts Tirion, forwards a decision, configures a webhook, or claims a correction. Run one interactive source census before any CC15C retry. |
| 2026-07-15 | 2.1.210 | `CC15C` TTY interactive late-decision attempt | One controlled Write attempt was visibly denied; the harness returned only a fixed closed-root-count result | No accepted start | No accepted terminal or late correction | No accepted root/child claim | No accepted usage/cost claim | One controlled denial was exercised; no accepted late-decision claim | No accepted file claim | No private profile, transcript, payload, identity, timestamp, or local log was inspected; only `native_decision_not_held_closed_root_trace_count` was retained as the result | Fail-closed and intentionally not retried. The count is not a decision-time-qualified topology result, so it cannot justify a harness or production change. CC15D now classifies the missing source topology before any separately reviewed CC15C reopen. |
| 2026-07-15 | 2.1.210 | `CC15C` piped `-p` source-shape negative control | The relay-held native-decision gate passed; the bounded transcript reader and one candidate reached the source-shape classifier | No accepted start | No accepted terminal | No accepted root/child claim | No accepted usage/cost claim | No accepted tool/decision claim | No accepted file claim | No private profile, transcript, payload, identity, or local log was inspected; the harness printed only `provenance_transcript_origin_kind_missing` | Expected fail-closed negative control. The harness explicitly uses Claude `-p`, whose local CLI contract is non-interactive. Do not infer a human/typed alias. The later TTY lane also remained fail-closed at a strict closed-root-count gate and is now paused pending a dedicated topology census. |
| 2026-07-15 | not re-queried | `CC15C` rerun after the broad provenance diagnosis | The relay-held native-decision gate passed; the bounded transcript reader and one candidate match reached the final provenance classifier | No accepted start | No accepted terminal | No accepted root/child claim | No accepted usage/cost claim | No accepted tool/decision claim | No accepted file claim | No private profile, transcript, payload, identity, or local log was inspected; the harness printed only `provenance_origin_not_human_typed` | Inconclusive. The final origin/prompt-source authority pair was not human/typed. It remains fail-closed; the next fixed category distinguishes missing/unrecognized origin or prompt-source shape from an incompatible known pair. |
| 2026-07-15 | not re-queried | `CC15C` relay-held rerun with direct-agent admission reduction | The relay-held native-decision gate passed; the receiver then reported no public lifecycle | No accepted start | No accepted terminal | No accepted root/child claim | No accepted usage/cost claim | No accepted tool/decision claim | No accepted file claim | No private profile, transcript, payload, identity, or local log was inspected; the harness printed only `provenance_unavailable` | Inconclusive. The fixed result proves only that trusted submission provenance remained unresolved. Reader/trust and matching failures are now emitted as separate fixed local-only categories; retry duration and authority stay unchanged pending that result. |
| 2026-07-15 | 2.1.210 | First and second separately authenticated `CC15C` live relay attempts | Both disposable-profile invocations passed profile preflight. The later fixed diagnostic reached the expected hook-cardinality checks before a relay rejection; no payload, transcript, identifier, path, or command value was retained | Not accepted | Not accepted | No accepted root/child claim | No accepted usage/cost claim | No accepted tool/decision claim | No accepted file claim | The harness discarded raw child output and teardown artifacts; only fixed failure categories were surfaced | Inconclusive, not a source-absence or authentication finding. The first result repaired matcherless `UserPromptSubmit`/`Stop` hooks and child-process OTLP initialization. The later coarse relay-rejection result prompted explicit generic and per-signal OTLP header/compression isolation plus self-tested fixed ingress, binding, target-status-class, and unsupported-metrics diagnostics. Rerun required. |
| 2026-07-14 | n/a (self-tested; no model invocation) | `CC15C` delayed native-decision relay acceptance harness | No Claude prompt was sent; relay, receiver, assertion, private-profile preflight, and slow in-flight seal fixtures exercised only synthetic in-memory request bodies | No live start | No live terminal | No live root/child claim | No provider usage/cost claim | The relay proves loopback/auth, exact denial-to-native-decision identity binding, closed-root trace counting, and a seal that waits an in-flight forward; the receiver/assertion prove root-span terminal evidence, correction ordering, strict no-execution claims, a seal that waits an in-flight authenticated handler, and clean post-seal counters through direct-agent fixed-point drain and exit | No live file claim | Synthetic canaries were absent from all status/control outputs; the preflight does not invoke a model | Harness readiness only. A real separately authenticated disposable-profile Claude run is still required before any `TelemetryIngress` or receiver acceptance claim. |
| 2026-07-12 | 2.1.201 | Phase 0 deterministic baseline | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Clean worktree; no capture | Reviewed baseline recorded without an exact commit SHA; `npm run check` passed 560 tests plus builds, TUI checks, and audit. |
| 2026-07-12 | 2.1.201 | Maintained Claude lifecycle read-only baseline | No prompt executed | No hook accepted | No terminal | 0 / 0 | 0 | 0 / 0 / 0 | 0 | Failed probe artifacts deleted | Blocked before model execution: Claude auth transitioned from `claude.ai` Pro to logged out. Harness must separate settings isolation from auth preflight and must never auto-login. |
| 2026-07-12 | 2.1.201 | `CC-13` exact-model terminal API failure | Same opaque prompt/session on `UserPromptSubmit` and `StopFailure` | Hook receipt only; successful start latency not exercised | Exactly one non-blockable `StopFailure`; no `Stop`; failed `run.ended.outcome` fixture | 1 / 0 | No successful tokens; accepted snapshot had failed Sonnet 5 plus one auxiliary Haiku title request; later pinned runs prove the Haiku request is optional | 0 / 0 / 0 | 0 | Metadata-only in-memory reduction; all content/path/error-detail/raw-ID canaries absent; no raw capture persisted | Accepted narrow native fact: `authentication_failed` ends the matching prompt as failure. Require exactly one requested-model failure and permit at most one failed Haiku title trace child; the title trace child is not a terminal invariant. |
| 2026-07-12 | 2.1.201 | Phase 2 source-readiness deterministic fixtures | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Prompt, assistant, tool-content, and raw-body gates default closed; legacy and current prior values restore | Enhanced/alias trace gates, explicit assistant-response gate, exact `StopFailure` hook, hook policy, v1 migration, stale/unrecoverable adoption, mixed handler preservation, drift, and restored-state reporting passed focused tests. This row predates successful native receipt. |
| 2026-07-12 | 2.1.201 | Pre-2.1.207 deterministic and live-failure verification | Matching submission/failure hook and prompt-log/root bridge | Hook receipt captured; successful start latency unavailable | `CC-13` passed with one requested-model failure and optional Haiku trace child; retrospective terminal remains explicitly delayed | 1 / 0 | No reported successful usage | 0 / 0 / 0 | 0 | Safe-only output; exact receiver/CLI permission parity; independent repo checks; no temp artifacts | `npm run check` passed 588 tests plus builds, Go TUI, census, and audit. The then-logged-out read-only preflight failed closed and left user settings unchanged. |
| 2026-07-12 | 2.1.207 | `CC-00` read-only root | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation pending | Read success; no skill/MCP/subagent | 0 | Safe-only output; no raw capture | Native read-only shape accepted; production request accounting still fixture-gated. |
| 2026-07-12 | 2.1.207 | `CC-01` structured Write/Read | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation not exercised in this native-only run | One successful Write and one successful Read | One exact safe relative causal-write candidate | Safe-only output; content/path canaries absent | Native structured-write shape accepted; this row predates the managed webhook proof below. |
| 2026-07-12 | 2.1.207 | `CC-03` rejected Read | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation pending | One denied PreToolUse; no completion hook | 0 | Safe-only output; rejected input absent | Native rejection is not execution. At the time, OTLP `tool_decision` integration remained pending; exact decision parsing and privacy regressions are now deterministic-gated, with a fresh live replay still required. |
| 2026-07-12 | 2.1.207 | `CC-04` non-zero Bash | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation pending | One Bash failure with native hook outcome | 0 | Safe-only output; command/output absent | Native structured Bash failure accepted; production tool dedupe remains pending. |
| 2026-07-12 | 2.1.207 | `CC-04I` interactive non-zero Bash, first managed attempt | One actual hook-authorized root plus one earlier shutdown-generated `provider_prompt_id` ghost start | Actual start delivered; ghost root never completed | Terminal versions 1 and 2 preserved identical time/totals | 1 / 0 for target | 4 input + 233 output = 237; cache read/create 50,232/11,213 | Hook copy was `unknown`, but a same-request OTLP wrapper incorrectly promoted Bash to success | 0 | Marker, command, absolute path, output, and file content absent; arithmetic/repository/queue checks passed | Failed acceptance. Fixes: every Claude shell log/trace wrapper now requires explicit process outcome, and Claude public start now requires `submission_hook`. |
| 2026-07-12 | 2.1.207 | `CC-04I-R` interactive non-zero Bash rerun | Exactly one `submission_hook` root; no provider-log ghost start | Start delivered in under one second | One closed-root terminal version; start/update/terminal time remained monotonic | 1 / 0 | 4 input + 143 output = 147; cache read/create 61,863/394; exact conservation | Hook and OTLP copies shared one request ID and both stayed `unknown`; one semantic Bash with `unknownCount=1` in every snapshot | 0 | Marker, command, absolute path, output, and file content absent; no causal files | Passed shell/start/privacy/accounting/ordering/queue acceptance. Five-minute post-completion stability check follows separately. |
| 2026-07-12 | 2.1.207 | `CC-28A` post-completion away-summary request | Reused the completed `CC-04I-R` query; no new accepted submission hook | No new public start or update | Existing terminal stayed at the exact original time and totals with no new version | 1 / 0 | A later same-request log/trace pair reported 94 input + 80 output with cache read/create 31,202/11, but all copies started after the explicit boundary and were excluded | No customer-visible activity revision | 0 | Safe upstream atoms retained only opaque metadata; webhook marker/content/path scans remained clean | Passed explicit-boundary acceptance: post-completion evidence did not change tokens, cost, activity, models, terminal time, or root count without guessing an auxiliary purpose. |
| 2026-07-12 | 2.1.207 | `CC-06` interactive three parallel Explore children, first managed attempt | Initial submission plus a provider-generated same-session wakeup each produced a `submission_hook` query | Two public starts leaked from one user request | Initial root never terminalized; wakeup root alone emitted one closed-root terminal | 2 public roots / 3 exact child hook identities | Initial root: 12 input + 936 output = 948, cache read/create 90,233/19,035; leaked wakeup: 4 + 227 = 231, cache read/create 66,868/1,682 | Three Agent hook/OTLP request pairs and three SubagentStart identities appeared as six rows; three child Bash calls stayed honest `unknown`; wakeup exposed one exact ScheduleWakeup failure | 0 | Marker, prompts, tool input/output, commands, absolute paths, and file content absent; repository correct; 19/19 outbox rows delivered with zero queue backlog | Failed lifecycle/lineage acceptance. Root causes: active same-session continuation prompt IDs were not aliased, Agent `tool_response.agentId` was discarded before its exact SubagentStart join, and descendant usage owners used wrapper span IDs instead of `tool_use_id`. Deterministic repairs require a live rerun. |
| 2026-07-13 | 2.1.207 | `CC-06-R` interactive three parallel Explore children, first repaired-binary rerun | The typed human submission and first background-authorized wakeup folded into the intended query, but two later `task-notification` / `system` transcript submissions became public hook roots | Intended start delivered in under one second; two post-terminal ghost starts leaked at +16 ms and +52 ms | Intended root emitted one closed-root terminal at 997 tokens, but stale running updates followed a 997-token settling update before terminal delivery 22.029 seconds after the provider boundary | 3 public roots / 3 exact child sessions on the intended root | Intended root: 18 input + 979 output = 997, cache read/create 133,450/43,581; one ghost later accumulated 94 + 90 = 184 tokens | Final semantic aggregate was three Explore plus three Bash, all honestly unknown; transient updates exposed four Explore rows; only two of three exact child usage owners were allocated while the `otel_event` owner was incorrectly unallocated and coverage still claimed complete | 0 | All 19 target deliveries bound `local/tirion_local_log_server`; marker, prompt, commands, arguments, output, paths, and content absent; files empty; queue drained | Failed acceptance. New exact native evidence distinguishes `origin.kind=human` / `promptSource=typed` from generated `task-notification` / `system` transcript records. Bounded provenance, atomic aliases, all-member child owners, honest coverage, and webhook high-water/coalescing are now deterministic-gated, but this failed row is not retroactively accepted and no final-binary live rerun has passed. |
| 2026-07-13 | 2.1.207 | `CC-06-RR` interactive three parallel Explore children, transcript-provenance rerun | All four synchronous `UserPromptSubmit` reads resolved unavailable because the matching transcript records were not visible until after each HTTP hook returned; later OTLP prompt logs persisted three `provider_prompt_id` occurrences | No hook-authorized live start; OTLP/rebuild published two lifecycle families about 58.6 seconds after the intended start | Intended root terminalized at 936 tokens; one generated-notification ghost terminalized first at 25 tokens and later revised durably to 198 while its delivered terminal remained stale | 3 provider-prompt occurrences / 3 exact child sessions; 2 public completed roots | Intended root: 14 input + 922 output = 936, cache read/create 107,871/27,433; ghost later reached 96 + 102 = 198 | Three Explore and three Bash remained unknown; intended terminal conserved 936, but Explore coverage was partial while one child-owned vector appeared under Bash and requires separate ownership review | 0 | Repository binding and empty-file behavior passed; marker and content were absent from public payloads; queue ended 0/0 | Failed acceptance. The deterministic fixture had written the transcript before invoking the hook, unlike live 2.1.207. Repair now defers only minimal non-content hook identity after returning 200, retries the bounded race-checked transcript read, and requires durable `submission_hook` authority for every Claude production root. No rerun has yet accepted this repair. |
| 2026-07-13 | 2.1.207 | `CC-06-RRR` interactive three parallel Explore children (`CC06RRR-20260712T235657Z`) | Human/typed transcript provenance resolved 116 ms after the synchronous hook; the generated task/system notification resolved after 108 ms and remained correlation-only | Exactly one hook-authorized public root; no notification or away-summary ghost root | Five monotonic terminal versions; first receiver terminal 3.494 seconds after the provider boundary and final correction 14.833 seconds after it | 1 public root / 3 exact Explore children | 544 input + 4,631 output = 5,175; cache read/create 586,625/79,344; root, grouped activity, and unallocated vectors conserved exactly; Explore coverage stayed honestly partial | Three exact Explore children and four real Bash calls; every Bash outcome remained `unknown`; MCP/skill/workflow not exercised | 0; repository remained unchanged | Marker, prompt/response, commands, tool arguments/output, paths, and file content absent from durable/outbound surfaces; queue drained | The repaired Claude core passed provenance, ghost exclusion, direct-parallel lineage, accounting, lifecycle ordering, repository binding, away-summary exclusion, and privacy. Release acceptance remains pending because 11 distinct updates shared one `updatedAt`, the default 1,000-event diagnostic snapshot rolled off within minutes, and audit identified structural cross-run head-of-line blocking. Provider-neutral fixes are deterministic-gated; one fresh rebuilt live rerun remains required. |
| 2026-07-13 | 2.1.207 | `CC06B-20260713T060835Z` rebuilt three-child observability stress | One human/submission root ran 06:10:09.692 to 06:10:28.870 UTC; trusted provenance resolved after 114 ms | Exactly one public root; no ghost root | Terminal versions 1/2/3 were monotonic; first arrived +3.066 s and final +5.267 s after the provider boundary; a request about three minutes later was excluded by the explicit boundary | 1 / 3 real background Explore children; Claude emitted three serial assistant messages, not the requested one-message parallel tool batch | 20 input + 1,895 output = 1,915; cache read/create 219,852/68,556; complete provider cost estimate $0.4260051; activity conserved as Explore 28 + ScheduleWakeup 1,352 + unallocated 535; four ownership conflicts failed closed | Each child made exactly Read plus the literal Bash command; three Bash outcomes stayed `unknown`; two unexpected ScheduleWakeup activities appeared; MCP/skill/workflow not exercised | No source mtime evidence; the pre-existing dirty baseline prevents a stronger no-write conclusion | Zero privacy matches; marker/content/path canaries absent; 344 safe windowed diagnostic events returned; outbox drained and no ghost roots | Bounded rebuilt core pass for strictly increasing logical `updatedAt` and inclusive diagnostic-window retrieval. It is not proof of the requested one-message Agent batch or cross-run lifecycle-anchor bypass, because launches were serial and no unrelated in-flight delivery was exercised; no release-readiness claim. |
| 2026-07-12 | 2.1.207 | `CC-05` direct subagent | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 1 direct hook identity | Requested Sonnet work plus exactly one Haiku session-title request; child token ownership pending | Agent success, one SubagentStart/Stop identity, child Read success | 0 | Safe-only output; raw agent IDs aliased | Direct hook linkage proven; trace `parent_agent_id`, recursive folding, and production lineage remain pending. |
| 2026-07-12 | 2.1.207 | `CC-15` skill control | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation pending | Read success; zero `skill_activated` | 0 | Safe-only output | Native skill absence accepted. |
| 2026-07-12 | 2.1.207 | `CC-16` direct skill | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One final Stop plus one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; skill token ownership pending | Skill success and exactly one `skill_activated` | 0 | Safe-only output | Native activation accepted; request-level `skill.name` attribution remains pending. |
| 2026-07-12 | 2.1.207 | `CC-11` blocked then final Stop | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | First Stop blocked with `stop_hook_active=false`, background/cron counts 0/0; later allowed Stop has `stop_hook_active=true`, background/cron counts 0/0; one closed interaction | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation not exercised in this native-only run | No tools | 0 | Safe-only output; Stop content absent | Native proof that the blocked Stop is non-final; the accepted implementation and managed root lifecycle are recorded below. |
| 2026-07-12 | 2.1.207 | `CC-12` background-paused Stop | One matched `UserPromptSubmit` and prompt-log/root bridge | Provider receipt only; Tirion `run.start` not exercised | One Stop with `stop_hook_active=false`, background/cron counts 1/0, and one closed interaction; no final completion | 1 / 0 | Requested Sonnet work plus exactly one Haiku session-title request; numeric conservation pending | One successful background Bash hook | 0 | Safe-only output; background description/command absent | Stop plus closed interaction is insufficient while background work remains. |
| 2026-07-12 | 2.1.207 | Managed full-stack read-only root | Exact managed prompt/session-to-trace bridge | One `run.start` followed by `run.update` | Matching ordinary Stop plus closed root produced `run.ended` versions 1 and 2 | 1 / 0 | Customer usage only; exact title request and same-request copies excluded | Read-only activity; no MCP/subagent | 0; no commit event | Lifecycle/privacy/auth metadata gates passed; no auxiliary identity or Haiku model left the webhook boundary | Read-only root completed without contributing identity or cost to either causal commit event. |
| 2026-07-12 | 2.1.207 | Managed full-stack multi-file writing root | Exact managed prompt/session-to-trace bridge | One `run.start` followed by `run.update` | Matching ordinary Stop plus closed root produced `run.ended` versions 1 and 2 | 1 / 0 | Customer usage only; `estimatedNanoUsd` and `usageValueNanoUsd` each 85,806,600 with complete cost coverage | Structured writing activity; no MCP/subagent | Multiple causal relative files and one writer-only commit event | Lifecycle/privacy/auth metadata gates passed; no auxiliary identity or Haiku model left the webhook boundary | Commit values exactly equaled this writing run and contained only its run identity plus attribution-relevant trace subset. |
| 2026-07-12 | 2.1.207 | Managed full-stack second writing root | Exact managed prompt/session-to-trace bridge | One `run.start` followed by `run.update` | Matching ordinary Stop plus closed root produced `run.ended` versions 1 and 2 | 1 / 0 | Customer usage only; `estimatedNanoUsd` and `usageValueNanoUsd` each 108,632,400 with complete cost coverage | Structured writing activity; no MCP/subagent | Causal relative write evidence and one writer-only commit event | Lifecycle/privacy/auth metadata gates passed; no auxiliary identity or Haiku model left the webhook boundary | Commit values exactly equaled this writing run and contained only its run identity plus attribution-relevant trace subset. |
| 2026-07-12 | 2.1.207 | Managed full-stack aggregate verification | Three exact managed root bridges | Three ordered start/update lifecycle families | Three roots each delivered terminal versions 1 and 2 | 3 / 0 | Three auxiliary request identities retained upstream and zero exposed; no Haiku model payloads | Root read/write slice; `CC-18` not exercised | Two causal commit events, each writer-only | 41 delivered, 0 queued, 0 blocked; lifecycle, integrity, privacy, authorization, and metadata gates passed | Full fixed-binary harness passed. Earlier read-only cost inflation and trace-direction assertion defects were fixed and regression-gated. |
| 2026-07-12 | 2.1.207 | Persistent managed-source repair and receipt | Fresh exact-Sonnet receipt through maintained source | Managed local receipt completed without hook error | n/a | 1 / 0 persisted root | 25 total tokens; complete cost coverage; exact title exclusion warning; Sonnet-only model set | n/a | n/a | Agent and database healthy; all eight hooks and enhanced-trace/redaction gates current | Doctor reports Claude measurement complete; no Haiku model is present. |
| 2026-07-12 | 2.1.207 | Pre-`CC-06-R` deterministic verification | n/a | n/a | n/a | n/a | n/a | n/a | n/a | Metadata census and privacy gates included | Historical gate: `npm run check` passed 602 tests, all builds, census, Go TUI checks, and high-severity audit with zero vulnerabilities. |
| 2026-07-13 | 2.1.207 | Post-`CC-06-R` deterministic repair verification | n/a; no live rerun | n/a; no live rerun | n/a; no live rerun | Deterministic one-root/task-target, three-child owner, and honest-coverage fixtures only | Deterministic token/activity conservation only | Bounded transcript provenance, atomic prompt aliases, all-member child owner aliases, Bash-to-child authority, conflict fail-closed behavior, and webhook high-water/coalescing | n/a | Bounded-tail path/symlink/race and content/raw-ID canaries included | `npm run check` passed 648 tests plus all builds, census, Go TUI checks, and audit. This is not live acceptance; child-first split, restart/eviction, nested ancestry, and the final parallel rerun remain pending. |
| 2026-07-13 | 2.1.207 | Post-`CC-06-RR` deterministic repair verification | n/a; no live rerun | n/a; no live rerun | Close-first and reordered Stop/task-notification sequences are deterministic-only | Deferred human/task/overlap and strict root-authority fixtures only | Stable request revisions reconcile once; conflict vectors conserve selected totals with every conflicted signal copy unallocated | Post-response provenance/close-settle barriers, minimal safe hook replay with durable retry, reserved/consumed-row freshness, continuation floor, durable ownership conflicts, and honest tool/subagent coverage | n/a | Prompt/tool/error/path canaries remain absent; retry buffers are bounded in memory and replay failures are isolated | `npm run check` passed 669 tests plus all builds, census, Go TUI checks, and audit with zero high-severity vulnerabilities. This is not live acceptance; a fresh final-binary parallel rerun remains required. |
| 2026-07-13 | 2.1.207 | Post-`CC-06-RRR` provider-neutral deterministic hardening | n/a; the later `CC06B-20260713T060835Z` row records bounded rebuilt live evidence | n/a; `CC06B` exercised the strict clock/window subset | Strict logical `updatedAt`, bounded lifecycle-anchor bypass, and shutdown behavior were deterministic-only at this checkpoint | n/a | Expanded update/terminal token totals retain matching `partial` cost coverage instead of inheriting an older smaller snapshot's `complete` coverage | Equal-source-time update ordering, unrelated in-flight delivery bypass, and inclusive windowed private-log diagnostics | n/a | Windowed diagnostics remain capped at 1,000 safe events across bounded rotations | The post-run fixes are regression-gated. `CC06B` later live-verified only strict logical update ordering and windowed diagnostics; its serial Agent launches and single-run delivery leave the requested one-message batch and cross-run bypass acceptance pending. |
| 2026-07-13 | 2.1.207 | `CC07R-20260713T062309Z` four-child terminal-latency stress | One human/typed submission with no task/away-summary public root | Exactly one public root; no ghost lifecycle | Exact Stop/closed-root authority; first terminal reached receiver +9.142 s after trusted source boundary | 1 / 4 exact high-confidence Explore children; short parent wrapper was not a fifth child | 26 input + 2,139 output = 2,165; cache read/create 309,756/63,344; Explore 1,178 plus unallocated 987 conserved exactly; estimate $0.2066223 with partial coverage | Four Read plus four Bash; every Bash stayed `unknown`; Agent launches were serial, not one tool batch | 0 claimed | Prompt/response/command/tool-data/path/file-content canaries absent; outbox drained | Topology, accounting, privacy, and ordered lifecycle passed, but first-terminal SLO failed. Closed-root authority arrived +4.140 s after source boundary; normal update head-of-line and same-run serialization added avoidable runtime delay. Dedicated terminal admission, immediate closed-root family projection, and durable queueing during in-flight update are deterministic hardening only; rebuilt live rerun pending. |
| 2026-07-13 | 2.1.207 | `CC07RR` terminal-priority live rerun | One human/typed submission root | Exactly one public root; integrity audit passed | Exact Stop/closed-root authority; source boundary to first receiver terminal was +13.622 s | 1 / observed child topology passed | 32 input + 3,495 output = 3,527; cache read/create 485,300/64,135; partial cost coverage | Five safe activities reported; no files claimed | 0 claimed | Privacy audit passed; no content/path canary in durable or outbound evidence; outbox drained | Failed first-terminal SLO despite integrity and privacy passing. Closed-root authority arrived +1.601 s, terminal durable queueing +4.423 s, but an unsent same-run settling update was selected first and terminal attempt began +9.733 s. Scoped priority is now implemented and deterministically verified for the fresh non-delayed Claude closed-root terminal (`codingHarness`/runtime `claude-code`, `evidence.basis: "root_span"`, `evidence.delayed: false`) after starts and ahead of fresh settling rows from the same or another run; it does not preempt an in-flight update, retains its same-run fallback on terminal retry/failure, and suppresses stale queued updates only after terminal success. Normal completed delivery remains ordered. Fresh live rerun pending. |
| 2026-07-13 | 2.1.207 | `CC07T-20260713T090706Z` native trace-interval acceptance probe | One human/typed, repository-bound submission root | Exactly one public root | Exact Stop/closed-root terminal; final root trace reached Tirion +5.012 s and first receiver terminal +5.574 s, missing the five-second SLO | 1 / 4 exact Explore children; launch timestamps do not certify one parallel assistant tool-call batch | Final run accounting conserved; no correction revision | Four Explore and four Read after cross-source deduplication; no Bash, wakeup, MCP, task/cron, shell, or write activity | 0 claimed | Prompt/response/tool-payload/path/diff/file-content canaries absent locally and at receiver; two outbox update suppressions were intentional | Integrity, privacy, receiver order, and post-boundary same-session containment passed. The native trace exporter still used its five-second default because only the generic BSP setting was managed. Source configuration v4 now manages `OTEL_TRACES_EXPORT_INTERVAL=250`; reopen and rerun required. |
| 2026-07-13 | 2.1.207 | `CC07U-20260713T105000Z` v4 native cadence acceptance | One human/typed, repository-bound submission root | Exactly one public root | Exact Stop/closed-root terminal; native trace reached Tirion +0.275 s, terminal queued +0.936 s, and receiver received terminal v1 +3.252 s | 1 / 0; no child topology requested | 2 input + 16 output tokens; no causal file or activity allocation | No tools, subagents, skills, MCP, shell, background, or write activity | 0 claimed | Prompt/reply/marker canaries absent from durable agent, log, outbound, and receiver artifacts; two extra updates were intentionally suppressed before HTTP | V4 source-latency acceptance passes with 1.748 s SLO margin. Receiver delivered start → update → terminal v1 on first attempt; no post-terminal delivery or correction. This no-tool run does not prove the exact one-message child batch, eligible late correction, or unrelated in-flight bypass. |
| 2026-07-13 | 2.1.207 | `CC07V-20260713T114904Z` four-child batch with clean exit | One human/typed, repository-bound submission root | Exactly one public root | No root Stop or StopFailure before/after exit; no terminal, terminal outbox row, or receiver `run.ended` was fabricated | 1 / 4 exact direct Explore children; native parentage proves siblings, while the Claude UI separately proves the requested one-message launch and one read per child | No terminal accounting projection | Four deduplicated Explore plus four Read; no Bash, writes, MCP, skills, wakeup, task/cron, or file claim | 0 claimed | Prompt/reply/marker canaries absent from safe durable, outbound, and receiver data | Batch/activity and privacy evidence pass; terminal acceptance is blocked by missing provider completion authority. V5 installs SessionEnd only as a safe diagnostic, never a terminal bridge. |
| 2026-07-13 | 2.1.207 | `CC07W` isolated bare `/exit` diagnostic probe | No user prompt or root; fresh bare Claude session exited at `2026-07-13T12:56:01.478Z` | No provider-hook safe observation or public start | One ignored `session_end` diagnostic with allowlisted normalized `prompt_input_exit`; no terminal authority | 0 / 0 public; one nonpublic OTLP log query occurrence | No production accounting | No accepted activity | No repository binding or file claim | Only bounded event category/reason retained; no raw IDs, content, or paths | V5 diagnostic-only exit behavior accepted. Concurrent native OTLP logs/traces failed closed for missing or ambiguous repository binding, so no public lifecycle, queue/delivery, or run webhook was created. Next: normal minimal repository-bound root/exit proof through exact Stop/closed-root authority. |
| 2026-07-13 | 2.1.207 | `CC07X-20260713T130423Z` normal minimal root plus clean exit | One human/typed, repository-bound no-tool root | Exactly one public root | Exact ordinary Stop/closed-root terminal; receiver order was `run.start` → `run.update` → `run.ended` v1 at 3.099 s → bounded `run.ended` v2 correction at 6.610 s; subsequent SessionEnd made no duplicate public lifecycle | 1 / 0 | 13 total tokens; bounded cost/coverage correction retained as terminal v2 | No tools, subagents, skills, MCP, shell, background work, write, or file claim | 0 claimed | Agent DB, structured logs, and receiver scan found no prompt/reply/path/content leakage; outbox drained | V5 coexistence with the ordinary exact terminal route passes. The correction was a versioned terminal replacement, not a duplicate retry or post-terminal update. This does not resolve the background-child missing-Stop gap from CC07V. |
| 2026-07-13 | 2.1.207 | `CC07Y-20260713T131748Z` one-child background-root exit | One human/typed, repository-bound root; its generated task notification resolved as correlation-only and folded into that root | Exactly one public root; it remained open | A background-bearing Stop paused the exact root and its physical close was suppressed; no later eligible ordinary Stop/closed-root pair arrived, so no terminal, terminal outbox row, or receiver `run.ended` was fabricated | 1 / 1 exact Explore child | 301 total tokens remained in the running projection; no terminal accounting claim | One deduplicated Explore plus one Read; no Bash, write, MCP, skill, wakeup, task/cron public activity, or file claim | 0 claimed | Agent DB, structured logs, and receiver artifacts had no prompt/reply/path/content leakage; outbox drained | Reproduced the missing-root-terminal capture gap at single-child scale. The task notification was correctly correlation-folded, not a ghost root. Next: a fresh one-child exit must show the additional bounded count-only `claude_background_root_missing_terminal` private diagnostic, with no durable observation, terminal, or webhook. |
| 2026-07-13 | 2.1.207 | `CC07Z-20260713T134947Z` one-child exit-diagnostic acceptance | One human/typed, repository-bound background-child root | Exactly one public root; it remained open | Exactly one private `claude_background_root_missing_terminal` warning with `prompt_input_exit` and count 1; one generic ignored SessionEnd diagnostic; no completion evidence, terminal row, production-terminal projection, or receiver `run.ended` | 1 / 1 exact Explore child | 475 tokens remained in the running projection; no terminal accounting claim | One deduplicated Explore plus one Read; 0 failures | 0 claimed | Agent DB, structured log, and receiver scans found no marker/prompt/reply/path/content leakage; queue empty | Receiver order was one `run.start` then six `run.update` records, with zero `run.ended` and zero duplicate identities. This accepts diagnostic visibility without elevating SessionEnd. Next: repeat a background-child root and manually `/clear`; expect an ignored `SessionEnd` clear receipt but no missing-terminal warning, terminal, or webhook. |
| 2026-07-13 | 2.1.207 | `CC07AA-20260713T140240Z` background-child normal completion then clear | One human/typed, repository-bound background-child root | Exactly one public root | First Stop reported one background task; a later ordinary Stop plus exact closed root terminalized the same root at `2026-07-13T14:11:40.419Z`; the later `/clear` SessionEnd was ignored and emitted no missing-terminal warning | 1 / 1 observed child | 1,424 reported tokens; normal terminal accounting projected only after exact completion evidence | Two tool activities; no file claim | 0 claimed | No duplicate event identity; outbox drained; no CC07AA privacy concern observed in safe audit | Receiver order was one `run.start`, five `run.update`s, then one `run.ended`. This accepts normal background-to-final-root completion under v5 and a post-completion clear. It does not accept the unresolved-background `/clear` control, because the final ordinary Stop arrived before clear. |
| 2026-07-13 | 2.1.207 | `CC07AB-20260713T142132Z` unresolved background-root manual-clear control | One human/typed, repository-bound background-child root | Exactly one public root; it remained open | A background-bearing Stop paused the root. A later ordinary Stop still lacked the exact closed-root half and remained nonterminal. Manual `/clear` produced one ignored `session_end` / `clear` receipt, zero missing-terminal warnings, zero clear-derived provider-hook observations, and no terminal | 1 / 1 observed child | 3,855 reported tokens before clear; no terminal accounting projection | 42 safe tool activities; no file claim | 0 claimed | Agent DB, structured log, and active receiver marker scans clean; outbox drained | Active receiver retained one `run.start`, 47 `run.update`s, zero `run.ended`, one identity, and zero duplicate event identities; every delivery preceded clear. This accepts the diagnostic-only clear boundary for an unresolved background root. Next: prove a new post-clear human root is isolated from the older unresolved root. |
| 2026-07-13 | 2.1.207 | `CC07AC-20260713T162236Z` post-clear sequential-root isolation | One fresh human/typed, repository-bound no-tool root after an older unresolved background root | Exactly one new public root, distinct from the older root | The new root completed at `2026-07-13T16:29:55.033Z` through its own exact ordinary Stop/closed-root pair. The older root remained open with no later activity, update, or terminal | 1 / 0 | Four usage atoms; no cross-root accounting | No tools or file claim | 0 claimed | Marker absent from agent DB/WAL, structured logs, and active receiver; outbox drained | Receiver sequence was one `run.start`, one `run.update`, and one `run.ended` with unique identities and no other post-clear Claude lifecycle. This accepts sequential isolation across clear; the next controllable gap is a user interrupt. |
| 2026-07-13 | 2.1.207 | `CC08A-20260713T163925Z` standard-subagent nested-delegation negative control | One human/typed, repository-bound root with one requested direct Explore child | Exactly one new public root; it remained open | One background-bearing Stop and later ordinary Stop remained nonterminal without an exact closed-root half | 1 / 1 direct child; no grandchild identity | No terminal accounting projection | One explicit direct Explore child, zero Read, zero claimed nested activity | 0 claimed | No ghost root or nested claim; outbox drained | Receiver retained one start and nine updates, zero ends, and zero duplicate/cross-root identities. Official Claude Code documentation states subagents cannot spawn subagents, so the absent grandchild is expected source behavior. Treat standard nested delegation as unsupported; test only agent-team/workflow surfaces for recursive lineage. |
| 2026-07-13 | 2.1.207 | `CC14E-001001` genuine user interrupt after local-server readiness | One human/typed, repository-bound submission root | Exactly one new public root | No Stop, StopFailure, SessionEnd, or `run.ended`; the root remained open | 1 / 0 | 157 reported total tokens; no terminal accounting claim | One Bash activity revised from `unknown` to explicit `failure` from native OTLP; no PostToolUse/PostToolUseFailure interruption fact arrived, so no `rejected` claim | 0 claimed | Agent logs, durable DB/journal/atoms, and six receiver artifacts had zero marker, command/output, or absolute-path matches; outbox empty | Narrow safety acceptance: a real interrupt did not become success or a terminal. Its non-success fallback is `failure`, not inferred `rejected`. The configured PreToolUse receipt was initially diagnostic `unknown`; the allowlisted diagnostic map now reports `pre_tool_use` without a durable activity or content retention. Explicit native interruption semantics remain open. |
| 2026-07-13 | 2.1.207 | Post-CC14E normal Bash / PreToolUse probe at 18:00Z | One fresh human/typed, repository-bound submission root after the prior session exit | Exactly one new public root | Stop remained only a nonterminal candidate; the exact request/root evidence completed the root. Receiver order was start → four running updates → completed terminal v1 at about +3.068 s, with no post-terminal update | 1 / 0 | Three successful LLM-request rows; no cost or completion claim beyond native request authority | One PreToolUse receipt was safely ignored as `pre_tool_use`; one PostToolUse and corroborating OTLP records produced exactly one Bash activity with `unknown` outcome, never an inferred success | 0 claimed | Scoped logs, durable DB/WAL, and six receiver artifacts had zero reported-marker, command-token, or absolute-path matches; outbox empty | Live acceptance of the repaired PreToolUse diagnostic contract and normal tool/lifecycle order. The unknown Bash outcome is correct because the native source exposed no explicit exit/status result. Explicit structured interruption/rejection evidence remains open. |
| 2026-07-14 | 2.1.208 | `CC14G` no-dialog permission-decision control | One fresh human/typed, repository-bound submission root | Exactly one new public root | No Stop, StopFailure, SessionEnd, or `run.ended`; the root remained open | 1 / 0 | No terminal accounting claim | One `pre_tool_use` diagnostic and one Write trace activity with `unknown` outcome; six accepted Claude OTLP log records yielded zero recognized decision activity because the then-current classifier excluded `tool_decision` | 0 claimed | Scoped agent, durable-state, and receiver scans were clean; outbox empty | Inconclusive, not a rejection acceptance. This exposed a parser gap, not proof that Claude omitted a native decision. “No dialog,” an absent local side effect, and missing later hooks do not establish denial, execution, or `rejected`. The deterministic repair accepts only exact `accept`/`reject` decisions with an opaque `tool_use_id`; fresh live acceptance remains required. |
| 2026-07-14 | 2.1.208 | `CC14H-18:19:46` side-effect-free nonzero-exit permission probe | One fresh human/typed, repository-bound submission root | Exactly one new public root | The exact request/root pair closed normally. Receiver delivery was one `run.start`, four `run.update`s, and two versioned `run.ended` projections; each unique delivery was sent once, with a duplicate terminal queue attempt suppressed idempotently | 1 / 0 | Native request accounting only; no decision-derived accounting claim | One Bash activity was durably `failure` from native tool-result evidence. The Claude OTLP-log receipt accepted 16 records and reported two pre-persistence activity atoms, but the safe durable projection retained no separate `accept` or `reject` decision state | 0 claimed | Scoped agent and recent receiver scans contained no reported marker, command token, or absolute-path match; outbox empty | Failure-path and ordered-delivery acceptance only. Pre-existing project-local Bash allow rules meant no visible permission denial was exercised. A nonzero result proves execution failure, never rejection or a permission decision; an explicit temporary Ask rule plus a visible user denial remains required. |
| 2026-07-14 | 2.1.208 | `CC14I-19:29:46` auto-approved permission control | Fresh operator submission; matching global allow policy auto-approved the tool and no Ask rule was active | Ordinary execution path only | Not accepted as a denial terminal or permission-decision assertion | 1 / 0 | No rejection-derived accounting claim | One generic Bash failure was safe-durable; no native rejection was asserted | 0 claimed | No rule, command, path, or decision-source value is retained or exported | Invalid rejection probe. It exposed that a native decision and generic same-tool result can collide on opaque identity; deterministic repairs retain separately addressable decision evidence, exact rejection priority, and no execution/file authority. A fresh Ask-forced visible denial remains required. |
| 2026-07-14 | 2.1.208 | `CC14J-20260714T082500Z` Ask-forced visible denial | Managed submission plus accepted Claude log/trace receipts; one non-authoritative lifecycle receipt was ignored | One running lifecycle received the safe activity revision | No Stop/closed-root authority arrived for the denied root, so no terminal was fabricated | Open root / 0 children | Request usage remained separate from the denied activity; no decision-derived usage/cost | Exactly one public Bash activity was `rejected`, with `failureCount=1`, `rejectedCount=1`, and no duplicate after its provisional unknown revision. The native decision had no duration/result/audit/artifact/token fields; an exact generic companion remained non-authoritative | 0 claimed | Durable, diagnostic, export, support, and receiver scans had no marker, prompt, command, permission-rule, path, or content match; outbox empty | Live native-rejection acceptance. A visible Ask denial is preserved as one safe rejected activity and never as execution, file, commit, usage, or terminal authority. |
| 2026-07-14 | 2.1.208 | `CC14K-20260714T112000Z` fresh no-tool isolation control | Fresh repository-bound Claude submission with accepted hook/log/trace receipts | One distinct public lifecycle | Exact closed-root evidence completed only the fresh no-tool root. Receiver sequence was one start, one update, and two strictly increasing terminal versions with no post-terminal update or duplicate identity | 1 / 0 | Provider request usage only; no cross-root accounting | 0 tools, 0 rejection/failure counts, and no inherited CC14J activity | 0 claimed | Source and receiver scans had no marker, prompt, command, rule, path, content, or forbidden-field match; queue empty | Live sequential-isolation acceptance. CC14J remained open and rejected while CC14K alone terminalized; neither activity nor terminal authority crossed roots. |
| 2026-07-14 | 2.1.208 | `CC15A-20260714T173700Z` / `CC15B-20260714T173900Z` controlled cross-run egress bypass | Two distinct repository-bound submissions; a transient local proxy held only CC15A's already-admitted `run.update` response open while CC15B began independently | CC15B receiver `run.start` arrived 593 ms after its trusted start time | CC15B trusted non-delayed `root_span` terminal v1 arrived 3.139 s after its boundary and v2 at 3.773 s; CC15A's held update did not reach the receiver until 139.999 s after CC15B v1 | 2 distinct roots / 0 children | No cross-root accounting assertion | One read-only activity per root; no writes, skills, MCP, or file claim | 0 claimed | Proxy retained request bytes only transiently for forwarding; recorded audit fields were safe event metadata only; normal receiver URL restored and proxy stopped | Live acceptance of the bounded cross-run lifecycle-anchor bypass. CC15B preserved its own start → update(s) → terminal v1 → v2 order while CC15A's in-flight request remained held. This does not exercise the separate late native-rejection/file/commit correction scenario. |
| 2026-07-14 | n/a (deterministic) | Connected exact-native late-decision external-egress gate | Directly constructed safe observations, a disposable watched Git repository, actual workspace attribution/dispatcher services, real Git reconciliation, and a metadata-only loopback receiver | One live V1 file claim was delivered before reconciliation | An exact native decision received later but sourced at/before `completedAt` produced a higher terminal revision with the exact file removed and superseded the dependent delivered commit; the same exact decision sourced after `completedAt` produced no correction | One deterministic query / no child topology claim | No provider usage/cost claim | Only opaque invocation/request/node/artifact identities and repo-relative receiver metadata were used; no prompt, response, tool argument/output, file content, raw telemetry, or absolute path was retained/exported | Exact provisional file claim only | Receiver retained safe event metadata only | Deterministic composition acceptance of the attribution/dispatch/Git/egress contract, including decision-only durable-attribution routing and malformed/post-boundary fail-closed fences. It is not live Claude emission or `TelemetryIngress` parsing/journaling acceptance. |
| | | | | | | | | | | | |

## Decision Log Template

Record every authority decision that affects customer truth.

| Date | Question | Native evidence | Decision | Owning construct | Tests/fixtures | Revisit trigger |
| --- | --- | --- | --- | --- | --- | --- |
| 2026-07-15 | May a piped `claude -p` submission with missing `origin.kind` be upgraded to a human/typed CC15C root? | The CC15C harness explicitly pipes its controlled prompt to `claude -p`; the installed Claude CLI documents `-p` as non-interactive. The relay-held source-shape rerun reached one candidate and returned only `provenance_transcript_origin_kind_missing`. No private transcript, value, or raw payload was inspected. | No. Preserve the exact Claude `origin.kind: human` plus `promptSource`/`prompt_source: typed` rule. Keep `-p` as a fail-closed negative control. Add a TTY-only manual-prompt acceptance lane to observe native interactive provenance, but do not infer human identity from a TTY; the same native evidence remains required. Interactive mode intentionally has no CLI `--max-budget-usd` cap because that option works only with `--print`. | `TelemetryIngress`, `RunCorrelationLedger`, `PrivacyGuard` | CC15C interactive-mode TTY guard, prompt-mode/preflight regressions, existing provenance source-shape matrix, and a fresh live TTY acceptance result | A TTY run emits a fixed provenance result, or Claude documents and a controlled native census proves a new explicit provenance contract. |
| 2026-07-15 | May `provenance_origin_not_human_typed` authorize a CC15C root or trigger an alias guess? | A subsequent relay-held CC15C rerun narrowed the unresolved result to the final origin/prompt-source allowlist after a bounded reader and one candidate match. It did not expose which field/value differed. | No. Preserve exact `origin.kind: human` plus `promptSource`/`prompt_source: typed` authority and correlation-only `task-notification` plus system. Split only local diagnostics into missing/unrecognized origin, missing/unrecognized prompt source, and incompatible recognized-pair categories. Do not retain values, infer aliases, or create a public root until native evidence proves a compatible contract. | `PrivacyGuard`, `TelemetryIngress`, `Diagnostics` | Origin/prompt-source failure matrix, existing snake_case `prompt_source` compatibility regression, ingress safe-diagnostic bridge, and CC15C reducer self-test | A fixed next result identifies a source-shape class, or a controlled native-source census proves a new exact provenance contract. |
| 2026-07-15 | How should an unresolved `CC15C` transcript authority be diagnosed before retry policy changes? | A relay-held native decision passed its exact gate, but the receiver saw no public run and the local safe reducer reported only `provenance_unavailable`. That establishes neither a late transcript append nor a path/trust, schema, digest, origin, or ambiguity cause. | Preserve the current bounded retry and all human/typed authority rules. Emit only one allowlisted non-content reason on unresolved provenance: transcript locator/trust/read state, bounded tail, hook identity, candidate/digest/origin state, ambiguity, or malformed provenance. Retain it solely in local `TelemetryIngress` diagnostics; unknown values collapse safely and never enter observations or webhooks. | `TelemetryIngress`, `PrivacyGuard`, `Diagnostics` | Transcript reader path/symlink/race fixtures; provenance-reason and forged-reason privacy regressions; CC15C safe diagnostic reducer self-test | A fresh result proves a matching human/typed record becomes available only after the current grace, or Claude changes transcript/hook source semantics. |
| 2026-07-14 | How may a live native-decision acceptance probe preserve the transcript authority it is trying to test? | A separate empty `CLAUDE_CONFIG_DIR` is unauthenticated, while `--no-session-persistence` prevents the persisted transcript that `UserPromptSubmit` provenance must resolve. Harness self-tests prove only the relay/receiver/control boundary. A third 2.1.210 run exposed that `OTEL_METRICS_INCLUDE_SESSION_ID=false` suppresses the fresh native session in logs and traces despite the setting name. | Require a user-created, separately authenticated, mode-0700 `mktemp`-style Claude root outside and disjoint from `$HOME` and the active normal root; reject symlinks, aliases, and containment before auth/model use. Run the direct agent and Claude child with that exact root, persist one synthetic session with a fresh UUID, and keep the raw transcript exclusively there. Set the session gate explicitly true only for the loopback probe; the relay and agent retain no raw session value in status/output. The relay forwards the real hook before issuing one controlled Write denial, buffers at most one early native candidate, and binds it only to the same exact session and opaque `tool_use_id`; prompt-ID disagreement is diagnostic-only because provider surfaces can omit, mismatch, or duplicate it. Require exactly one UserPromptSubmit, PreToolUse, Stop, and session-matched closed-root trace; no failure/completion hooks, no executed Write, no file/commit, unchanged Git HEAD, root-span terminal authority, and no post-terminal update. Normal profile metadata is compared without reading contents and loopback controls bypass proxies. | Acceptance harness; `TelemetryIngress`, `ExternalWebhookDispatch`, `PrivacyGuard` | `claude_cc15c_late_decision_acceptance_test.sh`, profile-preflight, delayed relay/receiver/assertion self-tests, and `npm run test:claude-census` | One real run must pass before any live claim; provider hook/OTLP schema, transcript-root behavior, correction-retention, or profile storage changes reopen the probe. |
| 2026-07-12 | Can an OTLP trace exporter alone make Claude trace-ready? | Current official monitoring contract requires both general telemetry and `CLAUDE_CODE_ENHANCED_TELEMETRY_BETA=1`; baseline settings lacked the gate. | Manage and restore the canonical enhanced gate. Report traces unavailable and `enhanced_traces_disabled` when it is absent even if exporter text is present. | `TelemetrySourceConfiguration` | `sourceConfiguration.test.ts` enhanced-gate configure/status/restore fixtures | Claude changes or removes the enhanced-trace gate. |
| 2026-07-12 | Should assistant response redaction rely on the prompt gate fallback? | Claude 2.1.201 supports `OTEL_LOG_ASSISTANT_RESPONSES`; when absent it inherits `OTEL_LOG_USER_PROMPTS`. | Set the assistant-response gate explicitly from the response-content policy and restore its prior absent/0/1 value exactly. | `TelemetrySourceConfiguration` | Assistant fallback, privacy-close, drift, and restore fixtures | Provider removes fallback or splits response/raw-body policy in a new contract. |
| 2026-07-12 | Is `StopFailure` part of managed Claude lifecycle and terminal authority? | Official hook contract says it fires instead of `Stop` for terminal API errors and cannot be blocked. Pinned 2.1.201 `CC-13` runs observed one matching `authentication_failed` hook, no Stop, exactly one requested-model failure, an optional Haiku title failure, and a closed interaction. | Configure and exact-shape check the hook. When prompt/session match the remembered submission, persist one provider-terminal failure, preserve its evidence/timestamp tuple monotonically, and publish optional provider-neutral `run.ended.outcome: "failure"`. Runtime authorization drops error detail and unmatched failures; separately, census evidence acceptance rejects unsupported model/count topology without making that topology a runtime gate. | `TelemetrySourceConfiguration`, `RunCorrelationLedger`, `TelemetryClassification`, `UsageProjection`, `ExternalWebhookDispatch` | Versioned two-request snapshot plus deterministic one- and two-span acceptance shapes; source, classifier, storage, projection, webhook, idempotence, evidence-time, and privacy tests | Native fixtures for other failure categories, restart between prompt/failure, or provider schema/version changes. |
| 2026-07-12 | Can a provider-terminal failure with no reported usage survive restart? | Native `CC-13` produced a terminal hook and failed request metadata but no successful usage atom. | Project one zero-usage completed run only from a customer-visible authoritative outcome occurrence. Keep model/cost unavailable, mark recovered delivery delayed, retain the original high-confidence terminal time, and bound rebuild by epoch plus recent terminal time. Retention advances the projection floor atomically. | `UsageProjection`, `RunCorrelationLedger`, `ExternalWebhookDispatch` | Immediate/restart, clean epoch, bounded lookback, retention, ordering, coverage, and privacy regressions | Provider exports authoritative failed-request usage or the production retention contract changes. |
| 2026-07-12 | May Tirion rotate authorization on an adopted Claude field whose original secret is not in the restore ledger? | The safe restore ledger records presence and ownership, not opaque header values. Rewriting an adopted header or preserved handler would therefore be irreversible. | Fail before mutation when the desired authority differs. Continue to rotate fresh Tirion-owned headers/hooks, whose absent baseline is exactly restorable. | `TelemetrySourceConfiguration` | Restarted A-to-B adoption conflict, fresh A-to-B restore, mixed sibling order, divergent/missing auth, and secret-free DTO/ledger tests | An approved private reversible secret-baseline mechanism is added. |
| 2026-07-12 | May the 2.1.207 prompt/request/tool/skill/child identities be promoted directly? | The non-interactive matrix proves one prompt-hook/log/root bridge per run, canonical request identities, scenario-specific tool outcomes, one direct child hook identity, and skill activation presence/absence. The managed three-root acceptance proves the exact root prompt/session/trace/request path in production, but not recursive lineage, complete child/skill token ownership, interactive behavior, or cross-version stability. | Accept bounded conflict-closed root prompt/session/trace/request correlation for the tested non-interactive production slice. Keep the joins in-memory and restart-limited. Promote tool, skill, and child meaning only through their own fixture-backed exact joins. | `RunCorrelationLedger`, `TelemetryClassification`, `UsageProjection` | Nine retained 2.1.207 safe census runs, synthetic root fixtures, focused production tests, and three-root managed full-stack acceptance | Interactive-mode capture, restart-spanning join, recursive child fixture, or provider schema change. |
| 2026-07-12 | How should the optional Haiku session-title request affect customer usage? | Every retained 2.1.207 census run had exactly one `claude-haiku-4-5-20251001` request whose `api_request.query_source` was `generate_session_title`; its log/span shared one request identity and the span was a direct trace child of the root interaction. The accepted 2.1.201 failure fixture independently marks its failed title request with exact `operation.name=generate_session_title`. In the managed three-root acceptance, three auxiliary request identities remained upstream while zero identities or Haiku models reached webhooks. | Map only those fixture-backed exact native fields to provider-neutral `auxiliary_session_title` and exclude the proven request plus every same-request signal copy from customer runs and live webhook activity, tokens, models, context, cost, and trace attribution. Missing/unclassified purpose remains counted; arbitrary `query_source` and `operation.name` values are discarded. Never create a separate public run. Broader compaction/auxiliary policy remains pending. | `TelemetryClassification`, `UsageProjection`, `ExternalWebhookDispatch` | Purpose propagation, projection exclusion, unclassified-counted, auxiliary-only, trace-first correction, trace attribution, live lifecycle regressions, and managed three-root acceptance | Either native field changes, request identity becomes ambiguous, restart spans the join, or a product decision reports explicit overhead. |
| 2026-07-12 | What distinguishes blocked, background-paused, and final Stop? | `CC-11` observed a blocked Stop with `stop_hook_active=false` followed by an allowed Stop with `stop_hook_active=true`; `CC-12` observed one inactive Stop with background count 1 plus a closed interaction and no final completion. All three managed root runs delivered their terminal only from the eligible Stop/matching closed-root join. | A blocked, background-bearing, or cron-bearing Stop is not terminal, and `stop_hook_active` is not authority. An eligible ordinary Stop is a bounded in-process candidate that requires the matching closed root interaction. | `RunCorrelationLedger`, `TelemetryClassification`, `UsageProjection`, `ExternalWebhookDispatch` | Native `CC-11`/`CC-12`, deterministic candidate/reorder fixtures, focused production regressions, and managed three-root lifecycle acceptance | Interactive ordering differs, restart spans the join, scheduled continuations appear, or Claude changes the native shape. |
| 2026-07-12 | How should the failed first `CC-06` attempt handle scheduled same-session continuations and direct Agent child copies? | The live attempt exposed two roots for one logical request, six rows for three exact children, a discarded Agent response child ID, and descendant owners keyed to wrapper spans. It did not pass acceptance. | Keep accepted/pending/completed lifecycle query-scoped; never reopen a completed alias; release each sequential query only from its own terminal halves; keep an active submission hook authoritative over delayed telemetry; suppress a background physical close only through exact trace-to-native-prompt identity. Transiently hash exact Agent response child ID and bridge it to exact `SubagentStart`; discard all other response/input content. Use exact `tool_use_id`, a bounded parent-first trace ledger, a conflict-closed same-request owner map, and whole-component activity/request/child deduplication. A launch without exact closure remains open/unknown. | `RunCorrelationLedger`, `TelemetryClassification`, `UsageProjection` | Deterministic continuation, reordered pause, direct-child bridge, ownership-conflict, component-deduplication, open-launch, and privacy regressions | Required live `CC-06` rerun; child-first split ordering; restart or eviction across the join; recursive/nested `parent_agent_id`; provider schema change. |
| 2026-07-13 | Which Claude `UserPromptSubmit` hooks may authorize a public query after failed `CC-06-R`? | The failed live rerun showed that native transcript records distinguish the typed submission (`origin.kind=human`, `promptSource=typed`) from generated task wakeups (`task-notification`, `system`), while raw hook prompt IDs may be absent, mismatched, or duplicated. | Read only a bounded race-checked tail under the real configured Claude `projects` root, including custom `CLAUDE_CONFIG_DIR`, and resolve provenance in `PrivacyGuard`. Human/typed is customer authority; task/system is correlation-only; unavailable or ambiguous evidence fails closed. Preflight raw/transcript aliases atomically. A background Stop's query remains the task target across completion and a newer manual prompt until that newer query's own background Stop replaces it. | `TelemetryIngress`, `PrivacyGuard`, `RunCorrelationLedger` | Full `CC-06-R` lifecycle fixture; missing/mismatched/duplicate/ambiguous prompt IDs; bounded read, path/symlink/race, custom config root, atomic-alias, mapping-safety, and privacy canaries | Final-binary live parallel rerun, restart/eviction across the target, or native transcript schema/root change. |
| 2026-07-13 | How can transcript provenance authorize a live Claude hook when 2.1.207 appends the record only after the synchronous HTTP hook returns? | `CC-06-RR` produced four immediate unavailable classifications at 20 ms, 3 ms, 2 ms, and 2 ms after their transcript timestamps; the records were visible only after the hook responses. OTLP then bypassed the missing hook authority with three `provider_prompt_id` occurrences and two public roots. | Return 200 without retaining prompt content, keep only bounded session/prompt aliases, transcript locator, workspace locator, event type, attempt identity, and original observed time in memory, then retry the same race-checked tail read across a bounded grace period. Hold Claude OTLP behind that pending-provenance barrier and settle closed interactions for 25 ms before joining it. An ID-less match must be within 250 ms; reserve each transcript row per attempt until durable commit, then mark it consumed. One pending submission may replay only minimal safe hooks with one durable retry, while overlapping unresolved submissions drop their ambiguous buffer. Resolved human/typed is appended with its original hook time; task/system remains correlation-only; final unresolved/ambiguous evidence is ignored. Independently, production projection accepts only roots with durable `submission_hook` evidence. | `TelemetryIngress`, `PrivacyGuard`, `RunCorrelationLedger`, `UsageProjection` | Post-response human/task retry, deferred task plus concurrent OTLP, close-first task race, sequential and overlapping identical prompts, overlap fail-closed, StopFailure/work-presence durable retry, privacy canaries, shutdown cancellation, provider-prompt suppression, full lifecycle regressions | Final-binary `CC-06-RR` rerun, transcript append latency beyond the bounded grace, agent restart during the grace, or provider transcript/hook ordering change. |
| 2026-07-13 | When may a merged Claude child claim descendant usage and complete coverage? | Failed `CC-06-R` had three exact children but allocated only two owners because one `otel_event` member alias was dropped, then incorrectly reported complete coverage. | Preserve every non-conflicting member activity ID in the exact connected component as an owner alias. Exact native Bash/descendant parentage may allocate through that alias set. Complete coverage requires every exact child to own usage; missing ownership is partial, conflicts are unavailable, and the remainder stays unallocated. | `TelemetryClassification`, `UsageProjection` | Trace/event owner permutations, three-child conservation, partial owner, conflict, Bash-to-child, and component-order regressions | Live parallel rerun; child-first split envelope; restart/eviction; recursive/nested ancestry. |
| 2026-07-13 | How must concurrent webhook projections behave after failed `CC-06-R`? | The failed live rerun delivered stale running updates after a settling update and delayed the terminal 22.029 seconds behind concurrent projection work. | Coalesce pending completed work per run, prioritize fresh terminal meaning over historical replay, durably admit terminal meaning without waiting for an update HTTP response, serialize only per-run HTTP attempts, and reconcile against the durable delivered update high-water. State/totals/activity/coverage cannot regress except an explicit source-pruning correction. Distinct running-update meaning gets a content-derived event ID; equivalent meaning reuses it and superseded unpublished rows are closed without stale delivery. | `ExternalWebhookDispatch`, agent runtime scheduling | Concurrent running/settling/terminal order, durable high-water, activity/token conservation, source-pruning, supersession, unique content IDs, terminal-priority, restart/delivery regressions | Final-binary live latency/ordering rerun or a change to the public replacement-snapshot contract. |
| 2026-07-13 | What provider-neutral release barriers remained after the `CC-06-RRR` core passed? | Eleven distinct delivered update meanings shared one `updatedAt`; the capped latest diagnostic snapshot displaced the run window within minutes; code audit found a fresh lifecycle anchor could wait behind an unrelated in-flight update. Independent review also found that an expanded partial-cost snapshot could inherit `complete` coverage from an older smaller snapshot. | Preserve equal provider source time in `evidence.observedAt`, but advance each distinct public replacement snapshot by a logical millisecond beyond the durable delivered high-water. Permit only start/terminal anchors to use a bounded parallel delivery claim while an ordinary response is in flight. Read live-audit diagnostics by inclusive UTC bounds from the capped active-plus-three-rotation private structured log, still returning at most 1,000 safe events. Select update and terminal cost evidence against the chosen totals and preserve partial coverage when those totals expand. | `ExternalWebhookDispatch`, agent runtime scheduling, `Diagnostics` | Equal-source-time and restart high-water ordering; expanded update/terminal partial-cost coverage; fresh-start in-flight-update bypass; bypass shutdown; diagnostic range, cap, rotation, CLI, and HTTP contract tests | `CC06B` later verified only logical update ordering and windowed diagnostics; a true one-message batch and a cross-run in-flight lifecycle-anchor test remain required. |
| 2026-07-13 | What does the rebuilt `CC06B-20260713T060835Z` run authorize? | One human/submission root, 114 ms provenance, three real background Explore children, three monotonic terminal revisions, strictly increasing public updates, an inclusive 344-event diagnostic window, zero privacy matches, and a drained outbox. Claude emitted three serial assistant messages rather than the requested one-message Agent batch; no unrelated run was in flight. | Accept this as narrow live evidence for the strict update clock and diagnostic window only. Do not promote it to one-message parallel-launch evidence or cross-run lifecycle-anchor-bypass evidence; preserve all broader release and program gaps. | `TelemetryIngress`, `PrivacyGuard`, `UsageProjection`, `ExternalWebhookDispatch`, `Diagnostics` | `CC06B` safe live audit plus existing logical-clock, window, bypass, shutdown, ownership-conflict, and privacy regressions | A live run with an actual one-message Agent batch and a separate fresh lifecycle anchor delivered while an unrelated update response remains in flight. |
| 2026-07-13 | How must a Claude terminal behave when the closed root arrives after a same-run update has begun delivery? | `CC07R-20260713T062309Z` retained one root/four exact children, clean privacy, and exact accounting, but its first terminal arrived +9.142 s after the trusted source boundary. The closed root was first observed +4.140 s late, then normal update work and same-run delivery serialization delayed terminal progression. | Keep the exact Claude Stop/closed-root join; do not promote Stop alone for latency. Admit terminal work through a dedicated path that never awaits outbound HTTP, immediately project the joined query family, and persist a terminal outbox row even while a same-run update attempt is in flight. Serialize only HTTP attempts and suppress post-terminal updates. Report provider emission, Tirion admission/queue, same-run dispatch, and receiver timing separately. | `UsageProjection`, `ExternalWebhookDispatch`, `RuntimeWorkScheduler` | Claude closed-root immediate-projection, terminal-queue-during-update, ordered-attempt, no-post-terminal-update, and timing-component regressions | Rebuilt live CC07R rerun meets the five-second source-boundary target while preserving exact authority and per-run order. |
| 2026-07-13 | How must a due terminal compete with an unsent same-run `settling` update? | `CC07RR` retained integrity/privacy and durably queued terminal meaning +4.423 s after the trusted source boundary, but an eligible settling update was selected before it. The first terminal attempt began +9.733 s and reached the receiver +13.622 s, failing the five-second target. | The implemented, deterministically verified rule gives fresh due selection precedence only to the trusted non-delayed Claude closed-root terminal whose webhook `codingHarness`/runtime are `claude-code`, `evidence.basis` is `root_span`, and `evidence.delayed` is `false`. It ranks after fresh starts and ahead of fresh settling updates from the same or another run in ordinary and bounded-bypass lifecycle selection. Do not preempt an update HTTP attempt already in flight; it completes under the existing per-run claim. If that terminal attempt fails or retries, retain the same-run settling row as a fallback. Suppress a still-pending stale update only after terminal delivery succeeds. This exception's public order is `run.start -> optional delivered run.update(s) -> run.ended`; every other completed lifecycle preserves its settling update before terminal among delivered snapshots. | `ExternalWebhookDispatch` | Due trusted-Claude-terminal versus same/cross-run settling selection, terminal retry/fallback, in-flight-update serialization, normal lifecycle-order preservation, stale-update suppression, no-post-terminal-update, and latency-component regressions | Fresh live rerun meets the five-second source-boundary target without weakening exact Claude authority or per-run serialization. |
| 2026-07-13 | Why did `CC07T-20260713T090706Z` still miss the terminal SLO with `OTEL_BSP_SCHEDULE_DELAY=250`? | The final closed-root trace reached Tirion about +5.012 s after the provider boundary while the generic setting was present at 250 ms. Inspection of the installed Claude Code 2.1.207 enhanced trace path shows that it reads `OTEL_TRACES_EXPORT_INTERVAL` directly and defaults it to 5000 ms. Tirion queued and delivered terminal meaning within about 0.57 s of that late receipt. | Keep exact Stop/closed-root authority and terminal dispatch priority unchanged. `TelemetrySourceConfiguration` v4 now manages the native `OTEL_TRACES_EXPORT_INTERVAL=250`, retains compatible `OTEL_BSP_SCHEDULE_DELAY=250`, validates and reversibly restores both baselines, and reports distinct native-interval and generic-batch drift codes. Invalid timing values fail closed. | `TelemetrySourceConfiguration` | Fresh/configure/restore, existing-value preservation, invalid/drift repair, v2-to-v4 and v3-to-v4 migration, malformed or inferred-v4-ledger fail-closed, cadence-only adoption, and full source-configuration regressions | `CC07U` accepted the reopened v4 source-latency path: closed-root receipt was +275 ms and first receiver terminal +3.252 s. Next probe must prove the exact one-message child batch; record any later eligible correction and cross-run bypass separately. |
| 2026-07-13 | Can Claude `SessionEnd` close a run when a background-child batch exits without root Stop/StopFailure? | `CC07V` received four direct child and four Read facts, then a clean Claude exit with neither root completion hook. `CC07W` subsequently received exactly one ignored `session_end` / `prompt_input_exit` diagnostic from a fresh bare exit, with no safe observation, production run, delivery, or webhook. `CC07X` independently proved that v5 coexistence preserves a normal exact Stop/closed-root terminal and emits no duplicate public lifecycle after exit. `CC07Y` reduced the gap to one child: its background Stop, correlated task notification, and suppressed physical close still lacked a later eligible Stop/closed-root join. Official `SessionEnd` is session-scoped, carries no prompt identity, and may fire for clear, resume, logout, or session switching. | Manage `SessionEnd` in source profile v5 for bounded local diagnostics only. On allowlisted `prompt_input_exit`, if active in-memory correlation still has unresolved background-paused roots, record only the capped count as private `claude_background_root_missing_terminal` visibility. The emitted diagnostic carries no root/session/query identity; bounded process-local keys are opaque and used only to count and clear live correlation. Do not read a transcript, bind a repository, append a durable observation, complete a query, or dispatch a lifecycle event. Preserve the exact ordinary Claude Stop/closed-root and matched StopFailure authorities. | `TelemetrySourceConfiguration`, `TelemetryIngress`, `RunCorrelationLedger`, `Diagnostics` | V5 fresh/migration/restore preservation tests; SessionEnd and missing-background-root diagnostic privacy tests; no-terminal classifier regression; positive later eligible-pair regression; normal root/exit coexistence and privacy audit | Fresh one-child background exit must show the count-only diagnostic and still no terminal/durable observation/webhook. Any future interruption authority still requires a versioned exact prompt/root join plus an interruption contract that cannot be confused with clear/resume/session-switch behavior. |
| 2026-07-12 | May a Claude commit inherit read-only run cost or unrelated trace identity? | The first live attempt exposed read-only cost inflation and a reversed trace-subset harness assertion. After correction, two commit events contained only writing run IDs and attribution-relevant trace subsets; each commit's estimated cost, usage value, and complete coverage exactly equaled its writer run. | Commit projection remains writer-only and uses attribution-owned run and trace subsets. Read-only roots contribute neither identity nor cost. The harness asserts directionality from commit facts back to the relevant writing run. | `GitAttribution`, `ExternalWebhookDispatch` | Two writing roots, one read-only control, exact cost/value equality, causal file evidence, trace-subset direction, and deterministic regressions | Multi-commit rewrite, ambiguous repository binding, or new provider write surface. |
| 2026-07-13 | How must Claude `SessionEnd: clear` behave while a background-paused root remains unresolved? | `CC07AB-20260713T142132Z` held one repository-bound background root open through a manual clear. Its background Stop and later ordinary Stop both remained nonterminal without the exact closed-root half. Clear produced one ignored SessionEnd receipt, no missing-terminal warning, no clear-derived durable hook observation, no terminal, and no post-clear delivery. | Keep `SessionEnd: clear` diagnostic-only and outside the `prompt_input_exit` warning path. It must not inspect a transcript, bind a repository, create a durable observation, complete either root, or dispatch a webhook. Preserve the older root's exact correlation independently so a later human submission has its own root and can be neither absorbed nor terminalized by stale evidence. | `TelemetryIngress`, `RunCorrelationLedger`, `ExternalWebhookDispatch`, `Diagnostics` | CC07AB safe live audit; clear reason filtering; no warning/terminal/durable observation; sequential-root isolation and stale-terminal-half regressions; privacy canaries | Fresh post-clear human root must prove distinct start/terminal identity while the older unresolved root stays open. Any future interruption authority still requires a versioned exact prompt/root join that cannot be confused with clear, resume, logout, or session switching. |
| 2026-07-13 | How must a fresh Claude submission after clear coexist with an older unresolved background root? | `CC07AC-20260713T162236Z` started a fresh no-tool human root after CC07AB clear. It completed through its own exact Stop/closed-root evidence and delivered one ordered lifecycle, while the older root remained open with no later activity, update, or terminal. | Scope every accepted, active, stopped, closed, and completed state to its exact opaque query/session identity. A new human submission is a distinct root; an older pending terminal half may not update, absorb, or complete it. Preserve the older root as open until its own exact authority arrives. | `TelemetryIngress`, `RunCorrelationLedger`, `TelemetryClassification`, `ExternalWebhookDispatch` | CC07AC live isolation audit; sequential-query, stale-stop/close, alias conflict, cross-root delivery, and privacy regressions | Standard nested subagents are unsupported; a supported team/workflow surface may reopen recursive validation. Restart/eviction across either root and any provider identity-schema change remain unsupported until separately accepted. |
| 2026-07-13 | Can an ordinary Claude Code subagent create a nested subagent lineage? | `CC08A-20260713T163925Z` yielded one direct Explore child, no grandchild identity, no grandchild Read, no terminal, and no ghost or duplicate root. The current official Claude Code subagent contract explicitly states that subagents cannot spawn subagents. | Treat standard nested delegation as an unsupported negative control. Preserve only the direct exact child and unknown/nonterminal root state; do not infer a grandchild from timing, task intent, or an unlinked OTel subagent event. Reserve any recursive lineage claim for separately supported agent-team/workflow evidence with exact native parentage. | `TelemetryClassification`, `RunCorrelationLedger`, `UsageProjection`, `Diagnostics` | CC08A negative-control audit; no-invented-grandchild, unlinked-child event, unknown outcome, and privacy regressions | A configured and natively evidenced agent-team/workflow surface may reopen recursive lineage validation. The next controllable gap is user interruption, whose missing Stop must not become a normal terminal. |
| 2026-07-13 | How should Tirion represent a real user interrupt when Claude provides no structured interruption flag? | `CC14E-001001` ran a project-native long-running process to visible readiness, then the user interrupted it once. The exact root retained one Bash native-tool outcome `failure`, but no Claude PostToolUse/PostToolUseFailure or explicit interrupted field arrived; there was no Stop, StopFailure, SessionEnd, or terminal. | Keep the root open and retain only the native `failure` fallback. Never infer `rejected` or success from UI text, timing, or a missing Stop. Classify configured PreToolUse only as the safe `pre_tool_use` diagnostic category; it cannot append an activity, bind a repository, or retain tool content. | `TelemetryIngress`, `TelemetryClassification`, `RunCorrelationLedger`, `ExternalWebhookDispatch`, `Diagnostics` | CC14E live source/receiver/privacy audit; pending-interrupted-hook replay regression; Claude shell outcome and PreToolUse diagnostic privacy regressions | A versioned native source exposes a structured interruption/rejection field, or a replayed safe interruption hook reaches ingress. Retry exhaustion remains a separate acceptance. |
| 2026-07-13 | What does a configured Claude PreToolUse receipt authorize? | The post-CC14E normal Bash probe emitted one PreToolUse receipt, which ingress reported as `pre_tool_use`, then one PostToolUse and corroborating OTLP tool evidence. Its exact root completed normally with one safe Bash activity and no duplicate delivery. | Treat PreToolUse as a diagnostic-only pre-execution category. It may prove that the configured hook surface is alive, but may not create a durable activity, repository binding, outcome, or content-bearing record. Only later explicit tool evidence can revise activity state. | `TelemetryIngress`, `PrivacyGuard`, `TelemetryClassification`, `Diagnostics` | Focused PreToolUse ingress privacy regression; CC14F live source/receiver/privacy audit | A provider version adds a structured pre-execution decision contract that can safely distinguish permission rejection from attempted execution. |
| 2026-07-14 | Can a missing permission dialog, absent local side effect, or missing later hook authorize Claude `rejected`? | `CC14G` produced a safe PreToolUse diagnostic and one Write trace activity with `unknown` outcome. Six accepted Claude OTLP logs produced zero recognized decision activity because the classifier then excluded documented `tool_decision`; raw payloads are intentionally discarded. | No. PreToolUse proves only pre-execution hook arrival; the trace proves only attempted activity. Never infer denial, execution, success, or `rejected` from UI state, filesystem absence, timing, missing telemetry, or an old projection that did not classify the native decision event. The repaired parser accepts only exact `accept`/`reject` with an opaque tool identity, maps `reject` to `rejected`, and keeps `accept` `unknown` until explicit result evidence arrives. It discards tool parameters, input, paths, content, and decision source. | `TelemetryIngress`, `TelemetryClassification`, `PrivacyGuard`, `RunCorrelationLedger` | CC14G source/receiver/privacy audit; exact decision parser, merge, and ingress privacy regressions; future live denial capture | A real visible-denial flow emits a versioned structured decision field that is captured end to end. |
| 2026-07-14 | What does an auto-approved, nonzero-exit Claude Bash probe authorize? | `CC14H-18:19:46` had one closed root, one native Bash `failure`, and fully idempotent receiver delivery. The project-local policy already contained Bash allow rules; no visible denial occurred. The OTLP-log receipt reported two transient activity atoms, but the safe durable projection intentionally retained no distinct decision name or value. | Treat it only as execution-result and delivery evidence. A nonzero exit is `failure`, not `rejected`; neither an allow rule, lack of a prompt, nor a transient atom count proves native `accept`. Do not persist or export rule text, command arguments, decision source, or parameters. `CC14J` subsequently supplied the separate Ask-forced rejection acceptance. | `TelemetryIngress`, `TelemetryClassification`, `PrivacyGuard`, `RunCorrelationLedger`, `ExternalWebhookDispatch` | CC14H source/receiver/privacy audit; decision parser, outcome merge, and webhook idempotency regressions | Revisit if the provider changes permission precedence or native decision/result contracts. |
| 2026-07-14 | How must an exact native Claude rejection coexist with a generic same-tool failure? | `CC14I-19:29:46` was auto-approved and is not rejection evidence, but it exposed a same-opaque-tool collision before a valid denial could be observed. Deterministic exact native-reject plus generic-failure replays cover both arrival orders and an incompatible child identity; `CC14J` then live-verified the rejection path. | Keep a native permission decision as separately addressable opaque evidence while sharing the exact opaque request identity with ordinary tool evidence. Only a validated native rejection with the same provider/query/nonempty request/kind/name outranks generic failure. Strip execution duration, result/token, audit, node, and artifact evidence from that semantic rejection; preserve its terminal meaning with `rejectedCount` as a subset of `failureCount`. A rejected write cannot authorize files or a commit: late correction requires a bounded opaque artifact/node pair from the exact successful semantic-write invocation; a generic causal key or grouped activity cannot substitute. | `TelemetryClassification`, `UsageProjection`, `TelemetryIngress`, `PrivacyGuard`, `ExternalWebhookDispatch` | Classifier, semantic-merge, durable-storage, ingress, live/terminal webhook, causal-file, privacy, and CC14J live regressions | Revisit if the provider changes the documented decision/result identity contract. |
| 2026-07-14 | May a denied Claude tool close its root or contaminate a later root? | `CC14J` produced one exact rejected activity and no terminal authority. The later tool-free `CC14K` completed from its own closed-root evidence, while the rejected root remained open. | A rejection is activity authority only. Never use it as Stop, closed-root, usage, file, or commit authority; scope it to its exact opaque root. A later root must start, update, and terminal independently, with no inherited rejection activity or counters. | `TelemetryIngress`, `TelemetryClassification`, `RunCorrelationLedger`, `UsageProjection`, `ExternalWebhookDispatch`, `PrivacyGuard` | CC14J/CC14K live audit; sequential rejected-root isolation regression | A provider adds a structured permission-decision terminal contract, or native identity topology changes. |
| 2026-07-14 | What prevents a native Claude reject from acquiring same-call usage or write authority? | Deterministic exact-invocation and connected external-egress composition now pass; `CC14J`/`CC14K` remain the live rejection and root-isolation evidence. | An exact `tool_decision: reject` is activity-only: it owns no descendant usage, accounting, context, or lifecycle authority. Exact cross-source matching prefers `invocationId` whenever either side has one and uses request identity only when both lack it. Ordinary durable file/commit projection needs a revalidated same-query/repository `{ artifactKey, executionNodeId }` proof; direct retained source-node proof is the sole provisional exception. A decision-only rejection execution-node observation is routed to durable attribution even without a query occurrence. | `TelemetryClassification`, `UsageProjection`, `WorkspaceChangeTracker`, `GitAttribution`, `ExternalWebhookDispatch`, `PrivacyGuard` | Exact-invocation, arrival-order, scoped source-prune, durable-proof, complete-census, duplicate/late-earlier-boundary, commit-supersession, runtime-routing, and privacy regressions; current full check passed | Revisit after a real provider decision traverses `TelemetryIngress` and the live receiver path. |
| 2026-07-14 | How is a late native Claude decision reconciled with an already sent provisional write/commit event? | Connected composition proves an exact late decision sourced at/before `completedAt` can remove its provisional file claim through a higher terminal version and supersede its dependent delivered commit. The same exact identity sourced after that boundary, received before the boundary, or malformed is ineligible. | Match with opaque `invocationId` whenever either surface supplies it; use request ID only when both invocation IDs are absent. During the existing 15-second terminal-correction retention, source-prune only the exact provisional activity/file proof whose source `startedAt` is at/before the retained explicit completion boundary. Durable grouped activity changes only on authoritative completed-run rebuild. External unreadable proof fails closed; duplicate/replayed completion rows retain the earliest valid boundary for correction authority even when a later row refines public terminal timing; a delayed earlier completion can only narrow a delivered live fence. Historical allocation revokes only from a complete census with `nativeRejectedCausalWriteArtifacts`. An already active commit event may later issue a `superseded` version, while independently valid sibling writers remain active. | `TelemetryClassification`, `UsageProjection`, `WorkspaceChangeTracker`, `GitAttribution`, `ExternalWebhookDispatch`, `PrivacyGuard` | Connected external egress, queued/delivered/post-boundary/malformed/decision-first/duplicate/late-earlier-boundary, commit-supersession, runtime-routing, and privacy regressions; current full check passed | Do not mark the program ready: genuine Claude provider-decision ingress/receiver delivery and a restart-spanning correction-boundary join remain required. |
| 2026-07-14 | May a fresh Claude lifecycle anchor bypass an unrelated in-flight update response? | `CC15A` held one already-admitted read-only root's `run.update` at a transient local forwarding proxy. While that response remained open, distinct read-only `CC15B` received `run.start`, ordered updates, and a trusted non-delayed `root_span` terminal v1 at +3.139 s (v2 +3.773 s). The held A update reached the receiver only +139.999 s after B v1. | Admit and snapshot lifecycle routing under a brief live-identity lock, then leave network I/O outside that lock. Preserve per-subject terminal tickets and same-run delivery order; permit only a confirmed exact in-flight same-run update to suppress a terminal-admission update. Never reparent after terminal selection, reservation, queueing, or delivery. | `ExternalWebhookDispatch` | Cross-run HTTP hold, terminal-ticket ordering, same-run update suppression, no reparent-after-terminal, concurrent admission, and live CC15 receiver audit | Any change to durable outbox claim semantics, proxy/receiver behavior, or lifecycle identity contract. |
| | | | | | | |

## Required Reading For The New Task

Local architecture and evidence:

- `AGENTS.md`
- `constructs.md`
- `docs/spikes/webhook-lifecycle-optimization.md`
- `docs/harness-run-monitoring.md`
- `docs/webhooks.md`
- `docs/spikes/claude-code-integrity-observability-outcome.md`
- `test-fixtures/claudeCodeNativeStopFailure.ts`
- `test-fixtures/complexChildAgentScenarios.ts`
- `packages/engine/src/complexChildAgentScenarios.test.ts`
- `packages/agent/src/externalWebhookDispatch.test.ts`
- `packages/agent/src/claudeNativeRejectionExternalEgress.test.ts`
- `packages/agent/src/productionRunAttribution.test.ts`
- `packages/agent/src/index.test.ts`
- `packages/agent/src/sourceConfiguration.ts`
- `packages/engine/src/telemetryClassification.ts`
- `packages/engine/src/shadowUsage.ts`
- `packages/agent/src/productionUsageService.ts`
- `packages/agent/src/externalWebhookDispatch.ts`
- `local-harnesses/claude_metadata_census_acceptance.mjs`
- `local-harnesses/claude_metadata_census_receiver.mjs`
- `local-harnesses/claude_metadata_census_receiver_self_test.mjs`
- `local-harnesses/claude_native_metadata_census_test.sh`
- `local-harnesses/claude_cc15c_late_decision_acceptance_test.sh`
- `local-harnesses/claude_cc15c_late_decision_preflight_self_test.sh`
- `local-harnesses/claude_delayed_decision_relay.mjs`
- `local-harnesses/claude_delayed_decision_relay_self_test.mjs`
- `local-harnesses/claude_delayed_decision_receiver.mjs`
- `local-harnesses/claude_delayed_decision_receiver_self_test.mjs`
- `local-harnesses/claude_delayed_decision_acceptance_assert.mjs`
- `local-harnesses/claude_delayed_decision_acceptance_assert_self_test.mjs`
- `package.json` (`npm run test:claude-census`)
- `local-harnesses/provider_native_activity_surface_test.sh`
- `local-harnesses/provider_native_skill_usage_test.sh`
- `local-harnesses/provider_skill_activity_stress_test.sh`
- `local-harnesses/tirion_local_server_dual_provider_lifecycle_test.sh`

Official provider contracts:

- [Hooks reference](https://code.claude.com/docs/en/hooks)
- [Monitoring and OpenTelemetry](https://code.claude.com/docs/en/monitoring-usage)
- [Subagents](https://code.claude.com/docs/en/sub-agents)
- [Tools reference](https://code.claude.com/docs/en/tools-reference)

## Final Operating Principle

The program should not ask whether Claude can be made to look like Codex. It
should ask what Claude Code natively proves, preserve that proof through the
shared Tirion constructs, and expose no more and no less certainty than the
provider evidence supports.
